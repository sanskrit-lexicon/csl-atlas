- **H5308 (OxAlpha `opencode/z-ai/glm-5.3-flash`) — review pool v1 machinery: the three blocked packets become double-keyed, blind, per-annotator review sheets with a validated two-annotator dry run.** Implements build items B1–B6 (+B9 tests) of [docs/REVIEW_POOL_V1_DESIGN.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/REVIEW_POOL_V1_DESIGN.md) (H5307, epic E014). **B1** — [validate_review_decisions.py](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/validate_review_decisions.py) `expected_sheets()` H4 fallback (needs-review rows if any, else the reviewed-ok set): the validator again expects the 89 rows the builder emits, after H1621 had emptied the needs-review filter and silently rejected every returned H4 export; the count pin in [test_validate_review_decisions.py](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/test_validate_review_decisions.py) moves 0 → 89 with the reason. **B5** — R2's first sheet: `r2_items()` renders the 10 checkpoint rows (diagnosis, review question, the row's closed `proposedParserLabels` vocabulary, archive-comparison details block, csl-orig source-record links), stem `csl-atlas-r2-checkpoint_10rows`; MG's recorded `reviewedValue` is deliberately never rendered (§9 honesty rule 3 — held-out expert baseline). **B3/B4** — pool mode in [build-review-sheets.py](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/build-review-sheets.py): `--pool data/review/pool_assignment.json --annotator pool-aNN` emits that annotator's slice to `review/pool/<id>/` with the pseudonym in the export's `reviewer` field (sheet_id stem unchanged — the filename/localStorage/validator contract), blind: the `Agent decision:` line is suppressed on H4 and no prior decision is rendered on any pool card (§3.1 item 2 — independence is the measurement); honest per-packet screening blocks (R2 0/0/0/10, H4 0/0/0/89, xref 10 auto-resolved/0/0/40) satisfy the H1649 `extras=True` gate that already blocked the three non-xref sheets. **B2** — validator `--pool` mode: the reviewer must be a manifest pseudonym (the hard-pinned `gasyoun` check retires in pool mode only), the item set must equal that annotator's assigned rows (1:1 slice check), and pool `defer` requires a note. **B6** — [build-review-pool-assignment.mjs](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/build-review-pool-assignment.mjs) (`npm run build-review-pool-assignment`) deals all three packets by the design's rotating-offset rule and commits the manifest [data/review/pool_assignment.json](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/data/review/pool_assignment.json) with schema [pool-assignment.schema.json](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/data/schema/pool-assignment.schema.json); the unanchored `.gitignore` `review/` pattern is root-anchored so `data/review/` is committable while the personal-artifacts `review/` dir stays local. **B9** — property tests: [test/review-pool-assignment.test.mjs](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/test/review-pool-assignment.test.mjs) (two distinct keys per row, load ±2, partner rotation, determinism; 4/4) and six pool-mode validator tests (13/13 unittest). **Two-annotator dry run** ([dry_run_review_pool.py](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/dry_run_review_pool.py), `npm run dry-run-review-pool`): n=2 pool (pool-a01/a02), R2 10/10, H4 89/89, xref 40/40 keyable rows dealt (packet coverage 10/105/50; 16 H4 + 10 xref auto-resolved rows excluded per design §3); both R2 pilot decisions files validate in pool mode and are committed as canary evidence under `data/review/pool_dry_run/` (synthetic approves, marked DRY-RUN — never promoted); H4/xref slices validate transiently and are not persisted. All six per-annotator sheets render (2 × {10, 89, 40}); blindness spot-checked (0 `Agent decision` lines, 0 prior-decision leaks). Publish-safety: pseudonyms only in committed artifacts, no contact-bearing strings, raw exports stay gitignored.
