- **A02/H5307 B1 (Claude Sonnet 5 `claude-sonnet-5`) — pool-review validator's H4 expected-set fallback landed; every H4 pool export can validate again.** [`scripts/validate_review_decisions.py`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/validate_review_decisions.py) `expected_sheets()` selected H4 rows by `reviewStatus == "needs-review"`, which H1621 emptied to 0 while [`scripts/build-review-sheets.py`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/build-review-sheets.py)'s `h4_items()` emits 89 `reviewed-ok` cards — every full H4 export failed `decided must equal the full sheet count` (surfaced by [docs/REVIEW_POOL_V1_DESIGN.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/REVIEW_POOL_V1_DESIGN.md) §3.1 item 1, B1 in the build table). The validator now mirrors the builder's own fallback (open rows if any, else `reviewed-ok`), so `csl-atlas-h4-semantic-field_89rows` resolves to 89 expected items instead of 0. [`scripts/test_validate_review_decisions.py`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/test_validate_review_decisions.py) count assertion corrected 0 → 89 and a new regression test (`test_h4_full_export_validates_against_the_reviewed_ok_fallback`) pins a synthetic 89-row export against the fallback set. Landed independently per the design's rollout note ("B1 is a live defect and should land first, independently of the rest"); B2–B9 (pool-mode reviewer check, blind rendering, R2 sheet, assignment builder, merge/adjudication, κ report, property tests) and the actual 5–10 student recruitment remain open — tracked as the roadmap item's owner residual in [ROADMAP_ATLAS_FAIR_PUBLICATIONS_2026_2027.md](https://github.com/gasyoun/SanskritLexicography/blob/master/ROADMAP_ATLAS_FAIR_PUBLICATIONS_2026_2027.md).
