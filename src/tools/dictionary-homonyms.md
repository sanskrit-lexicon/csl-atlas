---
title: Homonym splits
toc: false
---

# ${t("phase2.homonyms.title")}
Where validated homonym adapters **disagree on how many homonyms** a lemma has; i.e. one dictionary splits a form into several entries that another keeps as one.

> Deep comparison uses validated feature adapters only. Broad coverage/overlap covers eligible local Sanskrit/BHS headwords; missing deep markup is not counted as zero evidence.

## Trust Block

- Evidence: `src/data/dicts/homonym-split.json` and link-safe source pointers for validated homonym adapters.
- Limitations: homonym-count disagreement is an editorial-structure signal, not an error report or sense-alignment verdict.
- Validation: generated by `npm run build-dict-comparison`; checked by `npm run validate-dict-comparison` and `npm run build`.
- Owner repo: `csl-atlas`.
- Next use: inspect highlighted rows, then open exact dictionary source records before citing the pattern.

```js
import { slp1ToIast } from "../lib/lookup-normalize.js";
```

```js
const data = FileAttachment("../data/dicts/homonym-split.json").json();
const localesEn = FileAttachment("../locales-en.json").json();
const localesRu = FileAttachment("../locales-ru.json").json();
```

```js
const lang = view(Inputs.radio(["en", "ru"], { label: "Language", value: "en", format: d => d === "ru" ? "Russian" : "English" }));
const currentLanguage = lang === "ru" || lang === 1 || lang === "1" ? "ru" : "en";
const t = (key) => {
  const currentLocale = currentLanguage === "ru" ? localesRu : localesEn;
  const parts = key.split(".");
  let result = currentLocale;
  for (const part of parts) { if (result && result[part] !== undefined) result = result[part]; else return key; }
  return result;
};
```

```js
import * as Plot from "npm:@observablehq/plot";
```

<div class="note">
Homonym count = distinct <code>&lt;h&gt;</code> indices for the lemma in supported adapters. Dictionaries without validated homonym adapters are excluded, not counted as one or zero evidence. Differing homonymy is usually a legitimate editorial choice, not an error; this is an <b>analysis</b> view, not a correction queue.
</div>

```js
const includedHomonyms = data.includedDictionaries ?? [];
const unavailableHomonyms = data.unavailableDictionaries ?? [];
display(html`<div class="note"><b>Validated homonym adapters:</b> ${includedHomonyms.map(d => `${d.label} (${d.methodLabel})`).join(", ")}. <b>Unavailable for this metric:</b> ${unavailableHomonyms.length.toLocaleString()} broad dictionaries; they are excluded, not counted as one or zero evidence.</div>`);
```

```js
display(html`<p><b>${data.candidateCount.toLocaleString()}</b> lemmas where ${data.homonymDicts.join(", ")} disagree on homonym count; showing the <b>${data.shown}</b> with the widest spread.</p>`);
```

```js
function sourceChip(e) {
  if (e.href) {
    return html`<a href=${e.href} target="_blank" rel="noopener" style="margin-right:6px">${e.dict}</a>`;
  }
  const pointer = [e.sourcePath, e.line ? `L${e.line}` : ""].filter(Boolean).join(":");
  return html`<span title=${pointer} style="display:inline-block;margin-right:6px;color:var(--theme-foreground-muted);border-bottom:1px dotted var(--theme-foreground-muted)">${e.dict}${e.line ? ` L${e.line}` : ""}</span>`;
}
```

## ${t("phase2.homonyms.spread-of-disagreement")}

```js
const byMax = {};
for (const c of data.candidates) byMax[c.maxHomonyms] = (byMax[c.maxHomonyms] || 0) + 1;
const rows = Object.entries(byMax).map(([m, n]) => ({ maxHomonyms: +m, lemmas: n })).sort((a, b) => a.maxHomonyms - b.maxHomonyms);
display(Plot.plot({
  height: 240,
  x: { label: t("phase2.homonyms.max-homonyms"), tickFormat: "d" },
  y: { label: t("phase2.homonyms.lemmas-shown"), grid: true },
  marks: [Plot.barY(rows, { x: "maxHomonyms", y: "lemmas", fill: "var(--theme-foreground-focus)" }), Plot.ruleY([0])]
}));
```

## ${t("phase2.homonyms.widest-disagreements")}

```js
display(Inputs.table(data.candidates.map(c => ({
  [t("phase2.homonyms.lemma")]: slp1ToIast(c.lemma),
  ...Object.fromEntries(data.homonymDicts.map(d => [d, c.byDict[d] ?? "—"])),
  [t("phase2.homonyms.max")]: c.maxHomonyms,
  [t("phase2.homonyms.spread")]: c.spread,
  [t("phase2.homonyms.sources")]: c.examples
})), {
  columns: [t("phase2.homonyms.lemma"), ...data.homonymDicts, t("phase2.homonyms.max"), t("phase2.homonyms.spread"), t("phase2.homonyms.sources")],
  format: {
    [t("phase2.homonyms.sources")]: ex => html`${ex.map(sourceChip)}`
  },
  sort: t("phase2.homonyms.spread"),
  reverse: true
}));
```

---

Generated by `npm run build-dict-comparison`. CC-BY-SA-4.0.
