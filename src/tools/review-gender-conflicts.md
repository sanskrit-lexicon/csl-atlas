_Created: 29-05-2026 · Last updated: 05-09-2026_

---
title: Review · gender conflicts
toc: false
---

# Review queue · gender conflicts

Cross-dictionary gender disagreements among the tagged dictionaries (MW, AP, PWG, PWK, WIL) plus the prose lexica (VCP, SKD), as review reports. Each is a candidate for human judgment — read the inline entry, open the exact source line, and decide.

## This Queue Proves

This queue proves where dictionaries assert disjoint gender/POS evidence, or where parser conventions turn a dictionary-specific grammar signal into a conflict that needs review.

## This Queue Does Not Prove

It does not prove that one dictionary is wrong; many rows are legitimate convention differences or extraction limits.

## Trust Block

- Evidence: `src/data/review/gender-conflicts-review.json`, dictionary source pointers with inline snippets, and the review schema in `data/schema/review-report.schema.json`.
- Limitations: this queue samples conflict classes; it is not an exhaustive philological resolution of every gender disagreement.
- Validation: generated by `npm run build-gender-review`; checked by `npm run validate-review-reports` and `npm test`.
- Owner repo: `csl-atlas`.
- Next use: review a small source-linked sample while preserving the canonical review fields.

```js
import { slp1ToIast } from "../lib/lookup-normalize.js";
import { csvDownloadButton } from "../lib/csv-download.js";
import { sourceTextToIast } from "../lib/source-iast.js";
```

```js
const review = FileAttachment("../data/review/gender-conflicts-review.json").json();
```

```js
display(html`<div class="warning" label="Evidence: derived · review overlay">
Headwords are shown in <b>IAST</b> (the readable form); the raw SLP1 key is kept as a muted secondary. Gender is <b>derived</b> from <code>&lt;lex&gt;</code> tags (MW, AP, PWG, PWK, WIL) and <b>prose markers</b> (VCP, SKD); a conflict means two dictionaries assert disjoint specific genders. Each source pointer lands on the record that asserts the disputed gender and carries an inline snippet, so you never need to open a multi-MB file. This page reads the review overlay (<code>src/data/review/gender-conflicts-review.json</code>). To record a decision, set an item's <code>reviewStatus</code> / <code>reviewedValue</code> / <code>reviewer</code> in that file; rebuilds preserve it by <code>reviewId</code>.
</div>`);
```

```js
// Enrich every item once: IAST headword, a both-scripts search blob, and the
// flattened typology axes used by the filters, table, and CSV.
const items = review.items.map(it => {
  const slp1 = it.subject.lemma;
  const iast = slp1ToIast(slp1);
  const ty = it.machineValue.typology ?? {};
  return {
    ...it,
    slp1,
    iast,
    search: `${slp1} ${iast}`.toLowerCase(),
    genders: Object.entries(it.machineValue.byDict).map(([d, g]) => `${d}:${g.join("/")}`).join("  "),
    genderPair: ty.genderPair ?? "",
    basis: ty.basis ?? "",
    cardinality: ty.cardinality ?? "",
    overlap: ty.overlap ?? "",
    typologyLabel: ty.label ?? ""
  };
});
const uniq = (key) => [...new Set(items.map(x => x[key]).filter(Boolean))].sort();
```

```js
const statusCounts = {};
for (const it of items) statusCounts[it.reviewStatus] = (statusCounts[it.reviewStatus] || 0) + 1;
display(html`<div style="display:flex;gap:24px;flex-wrap:wrap;margin:8px 0">
  ${[["Total", items.length], ...Object.entries(statusCounts),
     ["lex-basis", items.filter(x => x.basis === "lex").length],
     ["prose-mixed", items.filter(x => x.basis === "prose-mixed").length]
  ].map(([l, v]) => html`<div>
    <div style="font-size:1.6rem;font-weight:700">${v.toLocaleString()}</div>
    <div style="color:var(--theme-foreground-muted);font-size:.85rem">${l}</div></div>`)}
</div>`);
```

```js
const statusFilter = view(Inputs.select(["(all)", ...Object.keys(statusCounts)], { label: "Status" }));
const pairFilter = view(Inputs.select(["(all)", ...uniq("genderPair")], { label: "Gender pair" }));
const basisFilter = view(Inputs.select(["(all)", ...uniq("basis")], { label: "Evidence basis" }));
const cardFilter = view(Inputs.select(["(all)", ...uniq("cardinality")], { label: "Cardinality" }));
const overlapFilter = view(Inputs.select(["(all)", ...uniq("overlap")], { label: "Overlap" }));
const lemmaFilter = view(Inputs.text({ label: "Lemma contains", placeholder: "IAST or SLP1", submit: false }));
const snippetScript = view(Inputs.radio(["IAST", "SLP1"], { label: "Snippet script", value: "IAST", format: m => m === "SLP1" ? "SLP1 (raw source)" : "IAST" }));
```

```js
const q = (lemmaFilter ?? "").trim().toLowerCase();
const filtered = items.filter(it =>
  (statusFilter === "(all)" || it.reviewStatus === statusFilter) &&
  (pairFilter === "(all)" || it.genderPair === pairFilter) &&
  (basisFilter === "(all)" || it.basis === basisFilter) &&
  (cardFilter === "(all)" || it.cardinality === cardFilter) &&
  (overlapFilter === "(all)" || it.overlap === overlapFilter) &&
  (!q || it.search.includes(q))
);
const shown = filtered.slice(0, 250);
```

```js
// CSV export of the current filtered view — IAST first, SLP1 kept as a key column.
function csvRows() {
  return filtered.map(it => ({
    lemma_iast: it.iast,
    lemma_slp1: it.slp1,
    genders: it.genders,
    gender_pair: it.genderPair,
    evidence_basis: it.basis,
    cardinality: it.cardinality,
    overlap: it.overlap,
    review_status: it.reviewStatus,
    reviewer: it.reviewer ?? "",
    sources: it.sourcePointers.map(p => `${p.dictionary} L${p.line} ${p.href ?? p.rawHref ?? ""}`.trim()).join(" | ")
  }));
}
const csvCols = ["lemma_iast", "lemma_slp1", "genders", "gender_pair", "evidence_basis", "cardinality", "overlap", "review_status", "reviewer", "sources"];
display(html`<div style="display:flex;align-items:center;gap:14px;flex-wrap:wrap;margin:6px 0">
  ${csvDownloadButton(csvRows, () => `gender-conflicts${filtered.length === items.length ? "" : "-filtered"}.csv`, csvCols)}
  <span style="color:var(--theme-foreground-muted)">${filtered.length.toLocaleString()} item${filtered.length === 1 ? "" : "s"}${filtered.length > shown.length ? ` · showing first ${shown.length}` : ""}</span>
</div>`);
```

```js
// Per-source pointer: IAST-labelled line, a link to the in-site line viewer,
// a raw fallback, and the inline snippet under a disclosure.
function sourceBlock(p) {
  const viewer = p.code && p.line ? `./source#${p.code}/${p.line}` : null;
  return html`<div style="margin:2px 0">
    <b>${p.dictionary}</b> <span style="color:var(--theme-foreground-muted)">L${p.line}</span>
    ${viewer ? html` · <a href=${viewer} target="_blank" rel="noopener">view line</a>` : ""}
    ${p.rawHref ? html` · <a href=${p.rawHref} target="_blank" rel="noopener" title="raw text (large file)">raw</a>` : ""}
    ${p.snippet ? html`<details style="margin:2px 0 4px"><summary style="cursor:pointer;color:var(--theme-foreground-muted)">snippet</summary><pre style="white-space:pre-wrap;margin:4px 0;padding:6px 8px;background:var(--theme-background-alt,#f6f6f6);border-radius:6px;font-size:.85rem;overflow-x:auto">${snippetScript === "SLP1" ? p.snippet : sourceTextToIast(p.snippet, p.code)}</pre></details>` : ""}
  </div>`;
}

display(html`<table style="width:100%;border-collapse:collapse">
  <thead><tr style="text-align:left;border-bottom:2px solid var(--theme-foreground-faint,#ddd)">
    <th style="padding:6px 8px">Headword (IAST)</th>
    <th style="padding:6px 8px">Genders</th>
    <th style="padding:6px 8px">Typology</th>
    <th style="padding:6px 8px">Status</th>
    <th style="padding:6px 8px">Sources</th>
  </tr></thead>
  <tbody>
  ${shown.map(it => html`<tr style="border-bottom:1px solid var(--theme-foreground-faint,#eee);vertical-align:top">
    <td style="padding:6px 8px"><span style="font-size:1.05rem">${it.iast}</span><br><code style="color:var(--theme-foreground-muted);font-size:.78rem">${it.slp1}</code></td>
    <td style="padding:6px 8px;font-size:.9rem">${it.genders}</td>
    <td style="padding:6px 8px;font-size:.82rem"><span title="gender-pair · cardinality · evidence-basis · overlap">${it.typologyLabel}</span></td>
    <td style="padding:6px 8px;font-size:.85rem">${it.reviewStatus}</td>
    <td style="padding:6px 8px">${it.sourcePointers.map(sourceBlock)}</td>
  </tr>`)}
  </tbody>
</table>`);
```

---

Generated by `npm run build-gender-review`; validated by `npm run validate-review-reports`. See [`docs/REVIEW_REPORTS.md`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/REVIEW_REPORTS.md). CC-BY-SA-4.0.

_Dr. Mārcis Gasūns_
