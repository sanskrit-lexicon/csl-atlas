# AGENTS.md — csl-atlas

This file orients Codex and other non-Claude agent sessions. The section between the generated-block markers is machine-owned (regenerated from Uprava; hand edits there are overwritten) — add repo-curated content OUTSIDE the markers.

[generated-block: repo-context — do not hand-edit; regenerated by Uprava tools/gen_repo_context.py; content-hash: d4d28d61f5]
## Shared org context (generated — H1393 layer)

- Canonical Sanskrit-domain facts (encodings, key1/key2, corpus gaps, transcoder traps): read the [Sanskrit context primer](https://github.com/gasyoun/github-spine/blob/main/SANSKRIT_CONTEXT_PRIMER.md) before touching encodings, transliteration, or corpus data.
- Task routing: [CLAUDE.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/CLAUDE.md) — open the matching section; do not read the file end-to-end. Sections: How to run · Key paths · Conventions · Do not touch.
- Danger facts (destroy-work-if-missed; full text + sources in [DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md)):
  - Never run the seeder with --reseed — it wipes human review overlays (R2 overlay-wipe; goals matrix G16 tracks it as destructive-risk)
  - H2892: --reseed is now gated in code — it exits 2 and writes nothing unless ALLOW_OVERLAY_WIPE=1 (exact string match). Do not set that variable to make a command work: the plain rebuild preserves every human decision and is the normal path. Lands with csl-atlas PR #411, open at the time of writing
  - pdftotext blanks Cyrillic while returning plausibly-shaped output - never poppler on Russian PDFs.
  - git pull / rebase --autostash in a shared clone can swallow another session's uncommitted work — prefer git fetch + a fresh worktree
  - Deleting opencode.db deletes all OpenCode chats - it is the session store, never treat as disposable.
- Further operational hazard notes live in the org-private hub ([Uprava DANGER_FACTS.md](https://github.com/gasyoun/Uprava/blob/main/DANGER_FACTS.md); org members only).
- Cross-repo data edges (from [Uprava interlinks_edges.tsv](https://github.com/gasyoun/Uprava/blob/main/interlinks_edges.tsv)):
  - cites → `csl-standards`: TEI/OntoLex-Lemon/FrAC export modelling ownership (live)
  - ← from `csl-corrections` (feeds): data/derived/correction_loci.tsv (39,540 locus-resolved records) (queued)
  - ← from `csl-orig` (feeds): all dict source texts for the cross-dict headword atlas (live)
  - ← from `csl-orig` (feeds): etymology_stats → src/data/etymology-oracle.json (live)
  - ← from `csl-guides` (vendors): src/data/atlas-extract.json (OBS-R/OBS-C figures) (live)
  - ← from `csl-guides` (vendors): citation graph + L2 cladogram → citation-sources.json/cladogram.json (live)
  - ← from `csl-observatory` (feeds): correction_events_release.csv (50,953 events) (queued)
  - ← from `VisualDCS` (feeds): DCS morphology feed for atlas mining (live)
  - ← from `ext:DharmaMitra` (feeds): GPU morphology/lemma runs (live)
  - ← from `SanskritSpellCheck` (feeds): nochange/do_not_file_suppress.txt (2,297 deliberate non-standard spellings) (live)
  - ← from `SanskritSpellCheck` (feeds): ortho-drift census → /tools/ortho-drift page (PH5 ORTHO-CLOCK) (live)
- Gitignored/off-git canonical assets are registered as pointer rows in the org-private [shadow-assets registry](https://github.com/gasyoun/Uprava/blob/main/SHADOW_ASSETS_POINTERS.md).

[/generated-block]
