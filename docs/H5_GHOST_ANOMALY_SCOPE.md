# H5 Ghost Entry And Anomaly Scope

Date: 2026-06-07

Status: scope note plus first classified review-queue sample. H5 remains a
strong testable hypothesis, not a release claim or automatic correction feed.

## Trust Block

- Evidence: `scripts/forensic/f0_shared_headword_typos.py`,
  `scripts/forensic/f2_structure.py`, `scripts/build-h5-anomaly-review.mjs`,
  `data/forensic/f0_report.json`, `data/forensic/f2_report.json`, and
  `src/data/review/h5-anomaly-review.json`.
- Limitations: most edit-distance-1 candidates are legitimate Sanskrit
  morphology or rare vocabulary, not proven errors. The first review pass
  separates correction targets, conventions, lineage signals, parser artifacts,
  and ordinary forms; it does not edit dictionary data.
- Validation: rerun `npm run build-h5-anomaly-review`, then
  `npm run build-h5-maker-qa-candidates`, then
  `npm run build-h5-maker-correction-proposal`, then
  `npm run validate-review-reports`.
- Owner repo: `csl-atlas`.
- Next use: hand off the generated
  [`H5_MAKER_CORRECTION_PROPOSAL.md`](H5_MAKER_CORRECTION_PROPOSAL.md) row for
  maker review; do not promote any automatic correction or broader ghost-entry
  claim from H5 alone.

## Scope Decision

H5 should not begin as "find all ghost entries." That would over-claim. It
should begin as a proof-first maker queue:

```text
rare shared anomaly signal -> candidate class -> human review -> QA target or lineage signal
```

## Candidate Classes

| Class | Source | Meaning | First use |
|---|---|---|---|
| `rare-near-core` | F0 normalized sanhw1 edit-distance candidates. | Rare lemma near a common lemma and shared by dictionaries. | Lineage signal; not an error claim. |
| `shared-doublet` | F0 pairs where both dictionaries carry rare form and common neighbor. | Stronger lineage signal because both forms coexist. | Review sample for possible inherited oddity. |
| `raw-headword-exclusive` | F2 raw `<k1>` pool shared only by MW plus one Petersburg dictionary. | Candidate raw spelling or headword anomaly. | Maker QA triage. |
| `homonym-concordance` | F2 homonym split agreement. | Structural inheritance signal, not a ghost entry. | Separate from anomaly review. |
| `known-correction` | `data/forensic/shared_corrections.csv`. | Already observed correction or editorial anomaly. | Seed examples and reviewer calibration. |

## Current Evidence

F0 reports 42,965 candidate rare near-core anomalies. The strongest lineage
counts are comparative, not absolute:

| Pair | Shared | Shared doublets | Reading |
|---|---:|---:|---|
| MW/PW | 12,113 | 11,506 | Petersburg-to-MW lineage signal. |
| MW/PWG | 8,195 | 7,714 | Petersburg-to-MW shared rare vocabulary signal. |
| PWG/PW | 9,451 | 8,685 | Same-author Petersburg baseline. |
| BOP/MW | 111 | 77 | Unrelated/null comparison. |
| SKD/BOP | 24 | 5 | Unrelated/null comparison. |

The manual caveat in `f0_report.json` is decisive: F0 measures shared rare
near-core vocabulary more safely than true typos. True ghost-entry work should
start from raw forms and reviewed samples.

## First Review Queue Shape

| Queue slice | Size | Purpose |
|---|---:|---|
| MW/PWG shared doublets | 30 | Separate inherited rare vocabulary from possible raw errors. |
| MW/PW shared doublets | 30 | Compare shorter Petersburg source against MW. |
| Raw MW+PWG exclusive pool | 30 | Find true spelling/headword anomalies from raw `<k1>`. |
| Null pair controls | 20 | Estimate false-positive morphology rate. |
| Known corrections | 20 | Calibrate reviewer labels against already observed edits. |

Implemented output: `src/data/review/h5-anomaly-review.json`, generated by
`npm run build-h5-anomaly-review`.

| Implemented slice | Items | Source |
|---|---:|---|
| MW/PWG shared doublets | 30 | `data/forensic/shared_headword_anomalies.csv` |
| MW/PW shared doublets | 30 | `data/forensic/shared_headword_anomalies.csv` |
| Raw MW+PWG exclusive pool | 30 | `data/forensic/raw_headword_pool.csv` |
| Null pair controls | 20 | `data/forensic/shared_headword_anomalies.csv` |
| Known corrections | 20 | `data/forensic/shared_corrections.csv` |

Current review status:

| Measure | Count | Reading |
|---|---:|---|
| Total review rows | 130 | Compact proof-first sample, not a complete anomaly inventory. |
| `reviewed-ok` rows | 130 | The first reviewer pass classified every row in the canonical review fields. |
| Observed calibration rows | 20 | Known corrections calibrate parser/tagging, convention, and typo labels. |
| Inferred candidate rows | 110 | Shared doublets, raw exclusives, and null controls are classified without changing source data. |

## First Review Pass Summary

The 2026-06-07 pass records only `reviewStatus`, `reviewedValue`, `reviewer`,
`reviewedAt`, and `note` in `src/data/review/h5-anomaly-review.json`.
`npm run build-h5-anomaly-review` preserves all 130 decisions by `reviewId`.

| Reviewed value | Count | Reading |
|---|---:|---|
| `legitimate-form` | 58 | The detector often catches real morphology, forms, or vocabulary. |
| `variant-convention` | 23 | Several rows are spelling, casing, stem, gender, abbreviation, or editorial convention differences. |
| `possible-typo` | 22 | A smaller subset remains useful as maker QA candidates. |
| `lineage-only` | 15 | Some raw MW/PWG exclusives are evidence of shared transmission, not correction targets. |
| `parser-artifact` | 12 | Known corrections often expose tagging or citation-segmentation cleanup rather than lexical errors. |
| `ghost-candidate` | 0 | No row in this compact first pass is accepted as a ghost candidate. |

| Slice | `legitimate-form` | `variant-convention` | `possible-typo` | `lineage-only` | `parser-artifact` | `ghost-candidate` |
|---|---:|---:|---:|---:|---:|---:|
| Known corrections | 0 | 2 | 6 | 0 | 12 | 0 |
| Null controls | 12 | 8 | 0 | 0 | 0 | 0 |
| Raw MW+PWG exclusive pool | 15 | 0 | 0 | 15 | 0 | 0 |
| MW/PWG shared doublets | 11 | 8 | 11 | 0 | 0 | 0 |
| MW/PW shared doublets | 20 | 5 | 5 | 0 | 0 | 0 |

## Maker QA Candidate Packet

Implemented output: `data/lexico/h5_maker_qa_candidates.json` and
[`H5_MAKER_QA_CANDIDATES.md`](H5_MAKER_QA_CANDIDATES.md), generated by
`npm run build-h5-maker-qa-candidates`.

The packet keeps the 6 known-correction `possible-typo` rows as calibration
controls, then ranks inferred shared-doublet `possible-typo` rows by the
smallest `nRealNeighbours` value. It emits 10 source-check rows: 5 with one
near-neighbor and 5 with two near-neighbors, split across MW/PW and MW/PWG.
The 2026-06-07 source-check pass marks 5 rows `source-supported-distinct`, 4
rows `source-supported-variant`, and 1 row
`source-declared-correction-candidate`: `divaraTa -> diviraTa`. These
source-check dispositions are review metadata only; they do not edit dictionary
data or accept a ghost-entry claim.

## Maker Correction Proposal

Implemented output: `data/lexico/h5_maker_correction_proposal.json` and
[`H5_MAKER_CORRECTION_PROPOSAL.md`](H5_MAKER_CORRECTION_PROPOSAL.md), generated
by `npm run build-h5-maker-correction-proposal`.

The proposal packet reads only `data/lexico/h5_maker_qa_candidates.json`,
selects the single `source-declared-correction-candidate` row, and cites the
MW/PWG source rows for `divaraTa`, the correction target `diviraTa`, and the
rejected detector neighbor `devaraTa`. The nine source-supported rows remain
excluded from correction flow. Maker decision fields stay empty/null; this is a
proposal for separate maker review, not an atlas dictionary edit.

## Review Order

| Order | Slice | First question | Expected reviewed values |
|---:|---|---|---|
| 1 | Known corrections | What does an already observed correction look like in this queue? | `possible-typo`, `variant-convention`, `parser-artifact` |
| 2 | Null controls | How often does the detector catch ordinary morphology or vocabulary? | `legitimate-form`, `variant-convention`, `parser-artifact` |
| 3 | Raw MW+PWG exclusive pool | Is the raw `<k1>` form a real headword, a source spelling problem, or an inherited bad form? | `legitimate-form`, `possible-typo`, `ghost-candidate`, `lineage-only` |
| 4 | MW/PWG shared doublets | Does the shared rare-near-core form behave like inherited vocabulary or a correction target? | `legitimate-form`, `variant-convention`, `lineage-only`, `possible-typo`, `ghost-candidate` |
| 5 | MW/PW shared doublets | Does the shorter Petersburg source show the same signal as PWG, or a different doublet pattern? | `legitimate-form`, `variant-convention`, `lineage-only`, `possible-typo`, `ghost-candidate` |

Keep `reviewStatus`, `reviewedValue`, `reviewer`, `reviewedAt`, and `note` in
the canonical review-report schema. The `queue` remains `encoding-ocr`; H5 is
identified by `reviewFamily: h5-ghost-anomaly` so the status vocabulary is not
expanded.

## Review Labels

| Label | Meaning |
|---|---|
| `legitimate-form` | Real Sanskrit form or normal morphology. |
| `variant-convention` | Orthographic or editorial convention, not an error. |
| `possible-typo` | Plausible source error, needs source inspection. |
| `ghost-candidate` | Plausible non-word or inherited bad headword. |
| `lineage-only` | Useful inheritance signal but not a correction target. |
| `parser-artifact` | Produced by normalization or extraction behavior. |

## Non-Goals

- No runtime LLM classification.
- No atlas-owned corpus or DCS frequency work.
- No automatic correction of dictionary data.
- No claim that F0 candidates are errors without review.

## Acceptance For H5 v1

The compact review report, preserved review fields, and first 130-row taxonomy
now exist. H5 v1 is complete as a proof-first classification package: it shows
that rare anomaly detectors produce many legitimate forms and conventions, a
smaller maker QA candidate set, and no accepted ghost candidate in this sample.
H5 stays Type 2 in the hypothesis index until follow-up source work converts
specific `possible-typo` rows into accepted dictionary correction proposals.
