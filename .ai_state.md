# Project Objective: Sanskrit Evidence Atlas

<!-- NB: local ../DCS/ export is reference data (text list + bibliography + 78k word→grammar list), NOT a passage corpus. DCS/corpus scope lives in ../VisualDCS per docs/BOUNDARY_RULES.md; the atlas may consume only a dictionary-facing summary per docs/VISUALDCS_CONSUMPTION_CONTRACT.md. -->

Build an interactive, documentation-first research atlas connecting corpus evidence, grammar, and Sanskrit lexicography, where every claim is traceable `Claim -> Evidence -> Source`. Static-first (Observable Framework). See `ARCHITECTURE.md`, `CHANGELOG.md`.

Status (2026-06-17): Phases 1–2 complete; Phase 3a done. Microstructure m1–m8 merged (PRs #31–#34). R2 page regeneration merged (main). VisualDCS adapter merged (main, dcs_lemma_summary.json + DCS band chip). Deferred engineering merged (PR #61): H1 panel viz refresh + sense-divergence map. MICROSTRUCTURE_DECISIONS round 12 merged (main). OBS-R/OBS-C corpus-evidence layer merged to main (PRs #53–#55) + `src/*.pdf` gitignore (PR #66). OBS-R + OBS-C written up as IJL-targeted manuscripts in `docs/articles/` (PRs #68/#69, merged). Subsequent work (referee-proofing, learner layer, FAIR citation/licence slices, P2 strengthening, R2 promotion experiment) recorded under Completed below.

**Dharmamitra integration layer merged (PRs #89–#96, #100, #102, #104, 2026-06-14):** review-queue consumers + shared `scripts/lib/dharmamitra_infer.py` + deploy fix. Real data: #90 source-layer anchoring, #95 PWG-markup, #100 compound-depth (live-API first pass, 2,173 verdicts of 3,496), #102 DTB link-target pilot (3,996 RV→VedaWeb verse links), #104 DTB link-splitting audit (corpus already atomised — only 2 of 311,933 MW citations combined). Still model-pending: gender (#89) + lemma (#92) because the live API's `lemma`/morphosyntax modes are degraded and there is no in-session GPU. See `docs/DHARMAMITRA_INTEGRATION.md` / `docs/DHARMAMITRA_MONTHLY_PLAN.md`.

**Broad dictionary parity + lookup polish merged (PR #106, 2026-06-14):** broad headword coverage/overlap now defaults to 40 eligible Sanskrit/BHS dictionaries while Core 7 remains stable for legacy/deep comparison. Deep metrics are adapter-gated by feature, Reader/Dossier lookup is IAST-only with deep-link support, source links are centralized and link-safe, and stable generated data avoids rebuild timestamp churn. Squash commit on `main`: `3c4646e05585ce7b008431006daf6f6de30d239d`.

**R2 hypothesis-rigor arc merged (PRs #123/#125/#126/#127/#129/#130/#132/#133/#134, 2026-06-15/16):** improved the R2 sense-inheritance hypotheses along falsifiability/stats/confounds/wording, each producing a concrete next test against committed data; then surfaced the results on the page, adjudicated the human-gated parser-promotion review, applied it, and kept the manuscript in lockstep — self-consistent end to end. **#123** H2 — naive cited-survival significance (0.762 vs 0.591, z=3.0 p≈0.002) corrected to OR 1.75, **p=0.16 (non-significant)** once centrality-controlled + lemma-cluster-robust + edge-FE; it was pseudoreplication. From-scratch JS logistic IRLS + CR1 sandwich in `build-r2-h2h3.mjs`; `h2Controlled` block + `data/lexico/r2_h2_senses.json`. **#125** H3R — the wil→yat "condensation" exemplar **retracted as a parser artifact** (YAT semicolon-packs ~5.7 meanings, not sense-numbered; splitter misreads class-number markers); `verify-yat-sense-artifact.mjs` → `r2_yat_artifact_check.json`. **#126** routed the fix as a reviewer-gated review packet (NOT a silent parser change): `build-r2-semicolon-counter-packet.mjs` → 26-row `r2_semicolon_counter_packet.json` + `docs/R2_SEMICOLON_COUNTER_REVIEW.md` (YAT→apply, SHS control→keep inline). **#127** H1R — replaced the bivariate Pearson r≈0 (underidentified: year = 11 dict-points over ~7 families) with within-family deltas (all 4 multi-dict families DECLINE, mean −0.11 units/lemma·yr), a near-saturated family-controlled OLS (year −0.051/yr, 95% CI [−0.186,+0.084] — bounds, not measures), and parser-regime stratification (western-only r=−0.11 → lexicographic, not parser artifact); `h1Controlled` block on `r2_h1_panel.json` + `r2_h1_panel_lemma.json` (330 obs). Sharpened claim: H1 is **contradicted in direction**, not merely unsupported. **#129** H2 threshold-sensitivity (referee item M4): swept the survival cutoff 0.10–0.25 and refit the #123 controlled model at each — the naive gap persists (0.11–0.17) but the controlled cited effect never reaches significance (OR 1.44–1.75, p 0.16–0.35) → the attenuation is robust, NOT a 0.15 artifact; `h2ThresholdSensitivity` block + surfaced the (previously page-absent) controlled result on the r2-h2h3 page (regenerating it also synced the page's H3R table/summary to #125's already-committed artifact flag). **#130** reconciled `docs/articles/paper_sense_inheritance.md` with all of the above (it had still printed the naive results): H2 downgraded (naive-significant p≈0.002 → confounded/non-significant under controls, answers referee M2); Wilson→Yates condensation exemplar retracted as a parser artifact ("abridged into Yates"); H1 reframed to contradicted-in-direction; abstract/§4/§5(retitled)/§6/§8/§9 + data-pointer note; docs-only +111/−42. Title "Condensation, Not Inflation" kept (now better supported — see #134). **#132** adjudicated the #126 packet (25 promote / 1 retain — lone-adjective `avazwabDa` retained; reviewer gasyoun) with per-row evidence notes, and hardened the generator to PRESERVE human decisions across rebuilds (by checkpointId; verified idempotent). **#133** APPLIED the promoted YAT semicolon counter (`splitSemicolon` + lone-adjective refinement; YAT_CONFIG.split="semicolon", SHS stays inline control): H3R wil→yat is now a genuine ~9→5.7 condensation (drift −3.3, overlap 0.26), artifact resolved. BUT recomputing exposed that H2's cited senses are 82/84 on ONE edge (ap90→ap; Wilson carries ~no `<ls>`), so the pooled controlled OR is edge-composition-unstable (the YAT split alone moved it 1.75→3.0). Added `withinPrimaryEdge` (clean single-edge test: 0.768 vs 0.661, z=1.80, p=0.07, NOT significant) + `citedByEdge`; H2's CONCLUSION is unchanged (co-varies, not established). **#134** re-synced the manuscript: Yates UN-withdrawn as a real condensation (3 sound edges now; strengthens 'copy or condense'), H2 reframed to the edge-concentration finding (uncited 0.591→0.705; naive pooled z drops 3.0→1.1), conclusion intact. All deployed/merged green; 189–190/190 tests (the lone failure throughout is the unrelated `sanskrit-util` sibling drift-guard from #119).

**Session arc + repo hygiene (PRs #146/#147/#149–#157, 2026-06-16/17):** NB this paragraph was lost once when a hygiene-sweep untracked `.ai_state.md` then restored an older copy (the journal must stay tracked — see #148/`ad3fa7c`); re-journaled here. **#146** DTB per-hymn verse table — `import-rv-verse-counts.mjs` snapshots VedaWeb's stanza index → `src/data/external/rv-verse-counts.json` (1028 hymns/10552 stanzas); the citation-link pilot validates verse EXACTLY per hymn (was ≤58 cap) → rejected 60 MW citations whose verse exceeds the cited hymn. **#147 + #153** P1 paper — *Measuring the Dictionary Family* scoped then fully drafted (`docs/articles/paper_measurement_framework.md`): 10-metric catalog + traceability layer (envelope/evidence-levels/trust-block/review-gate) + routing layer + AP90→AP worked example (76% containment, xref J=0.74/~85% mutual, drift −3). **P1–P6 now ALL drafted.** Atlas-only (measures dictionaries, not the project). **#149** builder idempotency — all 8 deterministic builders routed through `generatedAtForPayload()` (no more `generatedAt` churn; the unfinished half of #106). **#150** fixed 3 referee→paper links (stray `articles/` prefix). **#151** untracked the two generated site-data copies (`src/data/dcs/dcs_lemma_summary.json`, `src/data/lexico/sense_divergence.json`) — `sync-site-data` regenerates them from committed `data/` sources in prebuild (verified by fresh-checkout build + live deploy). **#154** ported the missing `## [1.0.1] - 2026-06-14` broad-dict/Dharmamitra CHANGELOG entry onto main (it existed only on the stale `docs/ai-status-changelog` branch, since deleted). **#157** fixed 36 move-induced broken links inside the archived LEGACY docs after the #148 hygiene sweep moved them to `docs/archive/` (`](X.md)`→`](../X.md)`, `](../data/X)`→`](../../data/X)`); 9 genuinely-dead cross-repo links (observatory docs now in `csl-observatory` + 1 placeholder) left as-is. **Repo health audit (read-only):** Observable production build PASS (130 links/51 pages); 179 FileAttachment refs, 73 npm scripts, 51 nav pages all resolve; no secrets; 0 TODO/FIXME in code; lexico m2–m8 + all validators green. The #150 referee links were verified NOT broken by the #148 move (referee + paper files both stayed in `docs/articles/`). **#159** triaged the remaining live-doc broken links: repointed `L0_HANDOFF.md`→`archive/SESSION_HANDOFF_LEGACY_OBSERVATORY.md` and `LEXICOGRAPHY_ROADMAP.md`→the moved `src/data/lexicographic-structure/L0/patel2016_assignments.csv`; de-linked genuinely-absent paths (`patel_fillin.csv` worksheet + `micro_gam.json`/`dict_profiles.csv`, whose generators were run locally and never committed). **0 broken links remain in live docs**; the only 9 left are in `docs/archive/` and point at `csl-observatory` docs (another repo) — unfixable here. Re-verified on current `main` (post-#160): Observable production build green again (130 links, exit 0), confirming the docs-only triage didn't touch the deploy path; tests 197/198 (lone benign `sanskrit-util` sibling drift), all validators green. Also confirmed the gitignored session journals (`HANDOFF.md`, `docs/SESSION_HANDOFF.md`) are intentional local working files (CLAUDE.md session-state protocol), distinct from the archived `SESSION_HANDOFF_LEGACY_OBSERVATORY.md` that L0_HANDOFF correctly references.

## ➡️ Next Steps (Queue)

**A09 (Sense Alignment) R2 harness + §1/§7/figures — DONE (2026-07-04, Fable 5 `claude-fable-5`, H152):** the handoff's premise ("R2 generator package missing") was STALE — the package was restored 2026-06-09 ([R2_REBUILD_CONTRACT.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/R2_REBUILD_CONTRACT.md)); this pass re-verified it against current csl-orig (all four generators re-run, byte-identical on re-run; drift third-decimal only, e.g. H2 cited 0.762→0.768, OR 3.04→3.30) and built only the true gap: **(1)** new deterministic figure generator [`scripts/build-r2-paper-figures.mjs`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/build-r2-paper-figures.mjs) (`npm run build-r2-paper-figures`) → `docs/figures/r2_fig1_h1_panel.svg` + `r2_fig2_alignment_anchor.svg` with §4's example rows PINNED (missing row = hard error) + 3 tests; **(2)** [PAPER_SENSE_ALIGNMENT.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/PAPER_SENSE_ALIGNMENT.md): §1 intro + §7 limitations drafted, stale 06-05 artifact note replaced, §4/§5 numbers moved to restored-committed values (archived kept as regression baselines), Figures + Reproducibility sections rewritten; **(3)** refreshed R2 data committed. **Honest flags kept open (not silently decided):** archived German↔English *dharma* example not reproduced (blocked on APPLYING the already-ruled `div-source-scope` labels — checkpoint review itself was done 2026-06-12, only the YAT semicolon counter is applied so far; agent-doable follow-up); H2 threshold-dependence must be reported as a sensitivity table; Stchoupak/French scope decision; §2/§6/§8 still outline-only; byline+venue MG @DECIDE. ⚠️ NB the older `docs/articles/paper_sense_inheritance.md` (P2/A02 arc, #123–#134) overlaps H2/H3 claims — any future edit must keep the two manuscripts consistent.

**Gender-conflict review tool overhaul — DONE (2026-07-03, Opus 4.8 `claude-opus-4-8`), MG-requested (4 asks):** reworked [`/tools/review-gender-conflicts`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/review-gender-conflicts.md) + set three general rules in [CLAUDE.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/CLAUDE.md) Conventions. **(1) IAST not SLP1** — headwords now render via `slp1ToIast` (SLP1 kept as muted secondary); retrofitted the 6 other SLP1-leaking pages found by a full sweep ([learner-reading-layer](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/learner-reading-layer.md), [sense-divergence](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/sense-divergence.md), [typology-treemap](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/typology-treemap.md), [mw-depth-dashboard](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/mw-depth-dashboard.md), [review-alignment](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/review-alignment.md), [h4-review](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/h4-review.md)). **(2) CSV download** on every table via new shared [`src/lib/csv-download.js`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/lib/csv-download.js) (Blob, IAST-first, UTF-8 BOM); wired into the gender page over the filtered set. **(3) Typology marker** — new deterministic [`scripts/lib/gender-typology.mjs`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/lib/gender-typology.mjs) classifies each case on 4 axes (gender-pair · cardinality · evidence-basis lex/prose-mixed · overlap), emitted into `machineValue.typology` + filterable + in CSV. Distribution: pair m/n 3084·m/f 1110·f/n 185·m/f/n 164; basis lex 2760·prose-mixed 1783; card binary 1217·multi 3326; overlap disjoint 3702·partial 841. **(4) Line-precise sources** — GitHub refuses to render the multi-MB `.txt` blobs (`#L` dies), so [`scripts/lib/source-snippet.mjs`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/lib/source-snippet.mjs) embeds a compact inline entry snippet in the review data AND new page [`/tools/source`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/source.md) streams the raw file, counting newlines and aborting at target+context (a line early in a 54 MB file costs ~1 MB). **Also fixed a real correctness bug:** the source pointer now lands on the record that asserts the *disputed* gender (was the first homonym record, which could show a different `<lex>` tag). Verified: `npm run build` green (57 pages incl. both new pages, no errors), `validate-review-reports` green, +4 typology unit tests pass (the lone failing test is the pre-existing unrelated `sanskrit-util` sibling drift-guard). Data JSON 6.2→12.6 MB raw / 1.5 MB gzip (inline snippets, as MG requested). Item count 4556→4543 (fresh rebuild from current csl-orig, no reviews lost — all were needs-review). **Not yet pushed** — on `main` locally, awaiting MG push/PR decision.

**Source-line IAST rendering (follow-up to #205) — DONE (2026-07-03, Opus 4.8 `claude-opus-4-8`):** MG flagged the [`/tools/source`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/source.md) viewer as unreadable (raw SLP1-inside-markup). New shared client renderer [`src/lib/source-iast.js`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/lib/source-iast.js) (`sourceLineToIast`/`sourceTextToIast`, reuses `from_slp1`) transcodes SLP1→IAST honoring each dict's encoding — MW `<s>…</s>`, PW/PWG/AP/WIL `{#…#}` (meaning `{%…%}` kept), VCP/SKD whole-line prose — and strips the markup shell. Verified on all 7 dicts (e.g. `{#aBAga#}¦, <lex>f.</lex> {#A#} <ls>TĀṆḌYA-BR. 6,7,5</ls>.` → `abhāga¦, f. ā TĀṆḌYA-BR. 6,7,5.`). Wired: viewer gets an IAST/Raw radio (default IAST); review-page snippets show IAST over a muted raw line. Rule added to CLAUDE.md Conventions. `npm run build` green (57 pages). **On `main` (merged via PR).**

**A10 article Fable review — DONE (2026-07-03, Fable 5 `claude-fable-5`, [PR #204](https://github.com/sanskrit-lexicon/csl-atlas/pull/204) merged):** hostile referee pass with 4 parallel fact-check agents (same model); verdict minor-to-major revision, ALL findings fixed same pass. Majors: References section added (paper had zero citations); §3.2 "occur nowhere else in the corpus" false for 65% of the 587 rows (criterion ≤4 lemmas, 203 unique) — restated; abstract "five signals / 43 dicts" → six / 41; §5 stale bootstrap 0.02/0.70–0.81 → 0.013/0.58–0.81 per current `data/L0/bootstrap_support.csv`; abstract 98% denominator made explicit (90/92 vs 123). Minors: favorable roundings (0.62/0.76, 9.5–49×), null-range omissions (BEN 0.10), thin-null caveats (PE n=3, AP n=8), Ahlborn–Scharf–Funderburk attribution, F6 floor/stratum precision, phantom companions (Articles 16/17) replaced with real links, §3.1 data pointer. Review: [A10_review_fable5.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/A10_review_fable5.md). A10 → 4/5; remaining gates: author byline + venue (DSH vs JCA); **queued for A11's review pass:** the same stale bootstrap pair sits in `paper_H_convention_vs_content_lineage.md` + `docs/L0_RESULTS.md` — re-verify there coherently, don't patch piecemeal. Data-side note: `ahlborn_mw_comparison.csv` row `u→utpulaka` has `mw_has_error=True` but status `mw_correct` (doesn't affect counts). The paragraph below predates [PR #202](https://github.com/sanskrit-lexicon/csl-atlas/pull/202) — §3.1's generator was recovered and reproduces byte-exact; see `docs/A10_REPRODUCIBILITY_AUDIT.md`.

**A10 (Apparatus, not errors) data + reproducibility audit — DONE (2026-07-03, Fable 5 `claude-fable-5`):** verified every A10 headline against committed forensic artifacts — F1 citation Jaccard (PWG→MW 0.1594, nulls BHS 0.0038/AP 0.0168), F5 citation-order (PWG 0.8107 / 47.8%, n=3,593; PW 0.73; BEN 0.68; AP 0.42), F4b decisive shared-error (2/123, null 256 vs 102.79, lift 2.491, p 4.01e-41), F3 gloss-length (0.5636 vs AP 0.5757), F6 gloss-translation (ALL .104/.129, VERB .044/.098, PHIL .086/.086), and the 17,007 MW∩PW-only count (reproduced exactly from `csl-observatory/observatory/snapshots/sanhw1.txt`) — **all EXACT**. **Found one reproducibility defect:** §3.1's size-corrected rare-lemma containment ratios (PWG→MW 0.70/0.82, PW 0.71, MW72 0.57, BOP 0.35) are UNREPRODUCIBLE — `scripts/L0/s6_content_lift.py` was NEVER committed (empty git log --all) and `data/L0/content_lift.csv` is absent, yet §7 claims figures regenerate from it. Faithful reconstruction from the snapshot reproduces the 17,007 exactly but no df-window/exclusion variant reproduces the published gradient (all give PWG 0.75–0.85, MW72 0.79–0.88, BOP 0.58–0.70). Wrote `docs/A10_REPRODUCIBILITY_AUDIT.md`; registered Type-1 `APPARATUS-NOT-ERRORS` hypothesis. **Article prose NOT touched.** **Queued author action:** recover/recommit s6_content_lift.py+content_lift.csv OR recompute §3.1 OR down-weight to the reproducible 17,007; fix §7. Decisive signals (F4b/F5) unaffected — the paper's core case is solid. Shared-erroneous-citation test stays out of scope (DCS/VisualDCS boundary). Prior Tier-1 passes: A01 #192, A03 #193, A04 #194, A06 #195 (all off origin/main).
**A06 (Order Is the Dictionary / kośa macrostructure) data + consistency pass — DONE (2026-07-03, Fable 5 `claude-fable-5`):** verified every figure against `kosha_macrostructure.json` (byte-stable regenerate) — ARMH 7,907 recs/860 verses/9.19 mean/56 Viṣṇu peak, ABCH 1,965 recs/4,619 eids/7 kāṇḍa/13,284 gender tags, Table 2 per-kāṇḍa counts, Table 3 all match exactly. **Found + fixed a real data-consistency defect:** A06 calls BOTH the 4,619 `<eid>` slots (Table 3) and the ~13.3k gender-tagged synonym-forms (§4.4) "lexeme", and A01/OBS-R counts the same abch at 11,584 — four different granularities, one word. Enhanced `m6_kosha_macrostructure.py` to emit `synonym_forms_total`/`synonym_forms_distinct` (ABCH 19,511 / 14,735) + an assumptions note naming all four units (records < eids < forms < OBS-R keys) and stating gender tags are per-form. Registered new Type-1 `KOSHA-MACRO` hypothesis row. **Article prose NOT touched.** **Queued paper edits when A06 opened:** (1) disambiguate "lexeme" in §4.3/§4.4 with the four named units; (2) state gender tags are per synonym-form; (3) cross-reference ABCH count with A01/OBS-R; (4) flagged secondary refs (Vogel, Wiegand, Svensén — needs MG). Amarakośa comparand BLOCKED on Meyer permission (acknowledge-gap is the only path; kosha [RELATIONS.md](https://github.com/gasyoun/kosha/blob/main/RELATIONS.md)). Prior A03/A04 passes rode branches/PRs #193/#194 off origin/main.

**A04 (Grammar Without Tags / indigenous microstructure) data + hypothesis pass — DONE (2026-07-03, Fable 5 `claude-fable-5`):** verified every headline against regenerated `indigenous_by_dict.json` + `root_agreement.json` — Table 1 root counts, §4.2 anubandha resolves (gaṇa 1,737 / pada 1,498), and §4.4 agreement (gaṇa 85.5% of 1,526, pada 75.3%, trans 81.4%) all reproduce EXACTLY; `root_agreement.json` byte-stable. Only drift: two control-dict totals (acc 49,822→49,833, mw72 55,388→55,390) + cosmetic KRM gloss-truncation noise (reverted, not committed). **Added the chance-corrected agreement the paper was missing** (`scripts/lexico/root_agreement_kappa.py` → `data/lexico/root_agreement_kappa.json` + `docs/ROOT_AGREEMENT_KAPPA.md`): homonym-free Cohen's κ per dict pair + modal base rate. **Graded result:** gaṇa robust (κ mean 0.81, range 0.58–0.91 vs 58.2% bhvādi base rate) — validates the core claim against the base-rate objection; but pada/transitivity only moderate (κ 0.55/0.53; base rates 63%/74%), YAT pairs weakest (KRM–YAT pada κ=0.03, at chance). The κ subset = single-label roots, so it also serves as the homonym-aware control the index flagged. M7-ROOT-AGREE hypothesis row updated. **Article prose NOT touched** (data/hypotheses-first). **Queued paper edits when A04 opened:** (1) report κ + base rate beside the raw % in §4.4; (2) scope "five conventions, one grammar" to gaṇa (strong), down-rate pada/transitivity to moderate; (3) fix stale mw72 total 55,388→55,390 in Table 1; (4) flagged secondary refs (dhātupāṭha/anubandha treatments, Vogel *Indian Lexicography*, measurement-bias metalexicography) — needs MG; (5) A35 lead-paper @DECIDE coordination. DharmaMitra gender/lemma cross-validation remains GPU-blocked. NB the A03 pass (three-axis independence) rode a separate branch/PR #193 — merge order doesn't matter, both off origin/main.

**A03 (Three Axes of Descent) data + hypothesis pass — DONE (2026-07-03, Fable 5 `claude-fable-5`):** verified every Table-1 figure against `data/lexico/three_axis_comparison.json` (byte-stable regenerate; double dissociation PWG→MW 0.893/0.371/0.013 and PWG→Schmidt 0.073/0.682/0.676 all exact). **Ran the independence test the paper was missing** (`scripts/build-three-axis-independence.mjs` → `data/lexico/three_axis_independence.json` + `docs/THREE_AXIS_INDEPENDENCE.md`): the 3 axes are mutually decorrelated (max |Pearson|=0.26, all n.s. at n=13) — empirically backs the "vector not scalar" thesis. **Two overclaims found:** bootstrap≈convention (r=0.92) so §3.2's "independent control" is wrong; register~layers collinear (r=0.85) so the composite micro axis is justified (not the cross-axis averaging the paper rejects). Promoted `THREE-AXES` from Type-2 (packet-only) to Type-1 `THREE-AXIS-INDEP` (tested) in HYPOTHESIS_INDEX. **Article prose deliberately NOT touched** (per MG: data/hypotheses first). **Queued paper edits when A03 is touched:** (1) add independence stats to §4; (2) reframe §3.2 bootstrap as within-axis confidence; (3) keep composite micro with the r=0.85 justification; (4) the flagged `[Author to add]` stemmatics refs (Pagel et al.; non-parametric bootstrap for tree support = Felsenstein 1985) + per-edge philological citations + non-independence caveat (13 edges, MW is child in 4) — needs MG scholarly input; (5) cross-paper: A03 jaccard has PWG 106,083 hw vs A01's new 106,082 (1-lemma snapshot skew) — reconcile. DharmaMitra gender/lemma cross-validation for A03 remains GPU-blocked.


**A02/A07/A08 revision execution — DONE (2026-07-02, Sonnet-tier session), pending author pass + SKD *iti* adjudication:** see Completed below for the full summary. Remaining is human-only: adjudicate the ~100-row [REVIEW_SKD_ITI_ADJUDICATION.html](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/REVIEW_SKD_ITI_ADJUDICATION.html) sheet (feeds A02 §8's fusion-classifier check and A08 C-M1's citational-vs-grammatical %), then the author pass on all three papers (byline, secondary-reference verification for the two flagged survival/decay citations, final read).

**Roadmap priorities (2026-06-17, user-set):** **(1)** Unblock Dharmamitra gender (#89) + lemma (#92) — GPU access is coming. **GPU-prep DONE** (`feat/dharmamitra-gpu-prep`): pinned the model revision `c0d2ada54f3d…` as the default `--revision` in `import-dharmamitra-morphology.py` + `scripts/lib/dharmamitra_infer.py`; added the two missing npm scripts (`import-dharmamitra-morphology`, `build-gender-model-crosscheck`) so the gender pipeline matches the lemma side; smoke-tested the offline scaffolding (load_lemmas/load_tag_map/IAST/head_gender all work — only the GPU forward pass is missing); wrote the push-button `docs/DHARMAMITRA_GPU_RUNBOOK.md`. **To activate:** on a GPU box run the runbook (≈4,556 gender + 1,913 lemma lemmas), commit the 2 snapshots + 2 regenerated queues. **(2)** Shrink the human-gated review backlogs via **auto-triage** (tighten generators so obvious rows self-resolve, leaving only ambiguous ones). **H4 DONE** (#164 + #165, `build-h4-review-packet.mjs` `applyAutoTriage` rule table): deterministic fold-match auto-resolve — `skd-false-low` missing row present under a looser `slp1_form_key` fold → `variant-headword`; `ap-ap90-delta` whose lemma is in AP90 under a fold → `normalization-risk` (the coverage delta is a normalization artifact, e.g. ananta→anaMta). → **16 of 105 auto-resolved with evidence (matchedHeadword), 89 left for human review** (15 variant-headword + 1 normalization-risk; the other 19 ap-ap90 deltas are real). The other 3 sample types (vcp-high-coverage / specialized-baseline / index-reverse-control) are all `covered`-quality judgement (thin entry / scope fit / reverse-index artifact) with no mechanical proof — left for human BY DESIGN. Conservative fold, keep+flag, overridable, never writes csl-orig; preserved/CI-safe path (auto-triage reusable from a committed packet, so the regen test passes on the csl-orig-less CI runner — the lesson from #164's first CI red). **Xref DONE** (#167, `build-xref-source-check-packet.mjs` `applyAutoTriage`): string-only rule — a cross-reference `target` carrying a CDSL truncation marker (abbreviation ring `˚` or trailing prefix `-`: a˚, mahA˚, su˚, vi˚, deva˚, a-, a/-, A-, a-s˚, aBi-) is a `prefix-convention` hub by the dictionary's own markup → **10 of 50 auto-resolved (all 10 prefix-controls), 40 shared-core lexical edges left for human** (their "is this MW/PWG edge meaningful?" is judgement; 0/40 carry a marker or self-fold). String-only → CI-safe by construction, no preserved-path needed. **Auto-triage initiative COMPLETE** — H4 + Xref were the only packets with genuine unreviewed backlogs. The other roadmap "targets" were already fully human-reviewed and needed no auto-triage (verified before touching them): R2-checkpoint 10/10 `reviewed-ok` (gasyoun, 2026-06-12); H5 anomaly-review 130/130 `reviewed-ok`; H5 maker-QA 10/10 `reviewed-ok` (codex; 5 source-supported-distinct / 4 variant / 1 correction-candidate); H5 maker-correction is the 1-row `divaraTa→diviraTa` submission to makers (a human action, not a backlog). **Live human work now = the auto-triage-surfaced needs-review sets: H4 89 + Xref 40 shared-core** — ordered evidence-first reviewer worksheets generated for both (#170, `npm run build-review-worksheets`): `docs/H4_REVIEW_WORKSHEET.md` (grouped by sample type, decision options + coverage per row) and `docs/XREF_REVIEW_WORKSHEET.md` (exact-edge rows first with clickable MW+PWG links + body excerpts; 4 missing-edge rows flagged). Pure derivation from the committed packets. **(3)** Papers **HELD** — P1–P6 all drafted, but do nothing toward submission until the author finalises byline + per-paper venue.



**1. Documentation & cleanup.** CHANGELOG.md now includes the 2026-06-14 broad-dictionary parity, lookup polish, and Dharmamitra integration updates. R2_PAGE_REGEN_SPEC.md marked COMPLETED. Branch docs/microstructure-decisions-r12 ahead of main by 1; cherry-pick or merge to main if ready.

**2. Pending human review & external dependencies:**
   - **R2 checkpoint review** — 10 rows need decisions in `src/data/review/r2-checkpoint-review.json` before any parser promotion. Guides: `docs/R2_CHECKPOINT_DECISIONS.md`, `docs/R2_CHECKPOINT_REVIEW.md`.
   - **P2 manuscript author tails (post-#139)** — `docs/articles/paper_sense_inheritance.md` is fully reconciled (R2 arc closed); its secondary references are filled (#137: Zgusta/Wiegand/Atkins&Rundell/Hausmann-HSK/Vogel + CDSL + the two companion papers, with §1/§2 inline anchors) and the pagination is verified against the publisher (#139: Wiegand 1989 corrected 409–462 → **409–461**, pinned HSK 5.1; the other works are whole monographs/handbooks whose details check out). Remaining are author-only, no code action: (a) byline/venue; (b) title "Condensation, Not Inflation" — well-supported now (three condensation/contraction edges, no inflation), keep unless the author prefers otherwise.
   - **P3 manuscript author tails (post-#141)** — `docs/articles/paper_three_axes_descent.md` (*Three Axes of Descent*, THREE-AXES methods paper) is drafted and registered. ~~(a) add the two flagged `[author to add]` references~~ ✅ **done by the H114 referee pass (03-07-2026, Fable 5 `claude-fable-5`)**: Felsenstein 1985 (bootstrap) + Andrews & Macé 2013 (multi-dimensional descent) filled and cited; Zgusta 1988/1971, Hanneder 2020, Wiegand 1989, Atkins & Rundell 2008 wired in-text (was zero in-text citations); Table 1 gained a Bootstrap column; the PWG→MW 1899 "register recoded" label contradiction fixed with a table note (register sim is 0.88 — recoding lives in convention 0.371 + layer Jaccard 0.5); P1 + OBS-R added to the companion map; full-URL links. All 13 edges' figures re-verified exact vs `three_axis_comparison.json`. Review: [A03_review_fable5.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/A03_review_fable5.md). Remaining author-only: (b) byline/venue. **A06 likewise done same pass** ([A06_review_fable5.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/A06_review_fable5.md)): all kośa counts re-verified exact vs `kosha_macrostructure.json`; TODO-references filled (Vogel 1979, Hausmann & Wiegand 1989, Svensén 2009, Zgusta 1971, Wilson 1819 flagged author-verify); stale "CDSL holds four koshas" updated for NMMB (fifth kośa, 2026-06, third digitization variant — strengthens §4.3); §4.3 cross-wired to P1 §3.2's lexeme normalisation. A03 + A06 → 4/5, remaining gates author-only (byline/venue; Wilson 1819 page check).
   - **P5 manuscript author tails (post-#143)** — `docs/articles/paper_xref_lineage.md` (*Pointing Inward*, XREF-CORE) is drafted and registered. ~~(a) add the two flagged `[author to add]` references~~ ✅ **done by the H119 referee pass (03-07-2026, Fable 5 `claude-fable-5`)**: Jaccard 1912 + Andrews & Macé 2013 filled and cited; Zgusta 1971/Wiegand/Atkins & Rundell wired in-text; **found + fixed an AP/AP90 edition-label SWAP in the abstract and Table 1** (609 edges belong to AP = the 1957 revision, 444 to AP90 = 1890; the paper had them backwards, plus raw counts 446/196 in a "(normalised)" table — normalised are 444/190); P1 §3.6 cross-cited; CAE-pair omissions footnoted; full-URL links. All pair figures re-verified exact vs `xref-lineage.json` + `xref_hub_review.json`. Review: [A05_review_fable5.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/A05_review_fable5.md). Remaining author-only: (b) byline/venue. **P4 likewise done same pass** ([A04_review_fable5.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/A04_review_fable5.md)): all root/agreement counts re-verified exact vs `indigenous_by_dict.json` + `root_agreement.json`; TODO-refs filled (Palsule 1961, Vogel 1979, Hausmann & Wiegand 1989, TEI Lex-0); "forty-three dictionaries" un-staled to 44; ACC (10 root hits, bibliographic catalog) hedged in Table 1's footnote; P1 §3.9 + A35/A30 coordination block added (lead-paper @DECIDE remains). A04 + A05 → 4/5; **all six P-series papers now ≥4/5, pending only MG author gates.**
   - **P1 manuscript author tails (post-A01 verification pass, 2026-07-02, Fable 5 `claude-fable-5`)** — `docs/articles/paper_measurement_framework.md` had its full A01 data-work pass: all 11 numeric claim families fact-checked against committed artifacts; three stale figures corrected (H2 survival 0.762-vs-0.591 → 0.762-vs-0.705 edge-concentrated per `data/lexico/r2_h2h3.json` — P1 had missed the #133/#134 re-sync; RV loci 3,996 → 3,942 per `src/data/review/citation-link-pilot-review.json` post-#146; corpus span "1822–1957" → print sources 1832–1993 per `data/dictionary_inventory.csv`); §3.4 iti triple replaced with committed `inlineItiTotal` values; the two flagged `[author to add]` references filled (PROV-O + Bender & Friedman; Peng + Sandve et al.) and a foundational-metrics related-work passage added to §7 (Zgusta 1988 copying study, Hanneder 2020, ELEXIS MWSA / Ahmadi et al. 2020, MUDIDI / Setiawan et al. 2026). Remaining author-only: byline, venue. **Queued data task DONE (2026-07-03, Fable 5 `claude-fable-5`):** both §3 anchors are now committed enveloped artifacts — `data/obs/citation_registers.json` (2026-07-02) and `data/obs/headword_collapse.json` (2026-07-03, generator `scripts/obs/headword_multiplicity.py` under the `dataset_meta` envelope, now format-aware for the `<s>`-less nmmb kośa new in csl-orig: 44 dicts, 1,496,157 entries → 410,259 distinct lemmas, 3.65:1, 42.2% unique; fold-sensitivity 38.8%, −3.4 pts unchanged). P1 §3.2 + data note and the HYPOTHESIS_INDEX OBS-R row re-synced. **Queued follow-up (agent-doable):** A07 `docs/articles/paper_redundancy_and_descent.md` still prints the pre-nmmb 43-dict figures throughout (1,495,459/409,649; 57.9/42.1%; mw 13.0, skd 37.2; "forty-three"; split-inflation 1,291,215) — needs its own careful figure re-sync against `data/obs/headword_multiplicity.csv` + `headword_collapse.json` when A07 gets its next pass (it is anyway parked pending author pass). `docs/CORPUS_REDUNDANCY_GENEALOGY.md` / `MICROSTRUCTURE_M1_M2_RESULTS.md` are dated snapshot findings — left as-is by design. **Hostile referee pass DONE (2026-07-03, Fable 5 `claude-fable-5`, H113):** verdict **MINOR REVISION**, all findings agent-fixed same pass — every §3/§6 numeric anchor re-verified clean against the committed artifacts; fixes: per-row §3 artifact links (the traceability paper now practices per-row traceability — was 2/10 rows walkable, endnote-only), OBS-R + OBS-C added to the companion map with per-metric ownership (anti-salami), 3 listed-but-uncited refs wired in (Atkins & Rundell §1, Wiegand §2, Zgusta 1971 §3.3), abstract/§4 mechanism-count harmonized (5→4), §3.5 verse-level base stated (5,682 of 15,916), corpus-span basis + 44-dict envelope anchor in §1. Review: [docs/articles/A01_review_fable5.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/A01_review_fable5.md). A01 → readiness 4/5; remaining gates author-only (byline, venue — DSH primary recommended).
   - **H4 source review** — 89 needs-review rows (16 auto-resolved); interactive voting sheet: `npm run build-review-sheets` → `review/h4_review_sheet.html` (PR #201).
   - **H5 maker correction** — ✅ CLOSED as no-csl-orig-edit 03-07-2026 (Fable 5 `claude-fable-5`): both source records are FAITHFUL apparatus entries (MW `<ab>w.r.</ab> for <s>divi-</s>`; PWG "falsche Form für diviraTa") — the wrong reading is printed intentionally, so a headword "correction" would violate the printchange/do-not-file discipline. The divaraTa→diviraTa linkage lives in the atlas packet; nothing to queue or post upstream.
   - **Xref source-check** — 40 shared-core rows (10 prefix-controls auto-resolved); interactive voting sheet: `review/xref_review_sheet.html` (PR #201).
   - **VisualDCS adapter wiring** — ✅ was already wired (#106/#151); PR #201 refreshed the feed to the 2026-06-13 emission and fixed the stale home-page prose.

**3. Issue #30 open tails** (lower priority): (a) `di0`/`sO0` gaṇa short-forms held for maintainer review; (b) YAT transitivity/seṭ decode "not systematically decoded" — source probe needed.

**Dharmamitra Months 1–2 (in progress):** *Month 1* — compound-depth (#91) ACTIVATED via #100 (ByT5 `unsandhied` on the live API; 2,173 verdicts of 3,496, ~38% degraded/junk filtered to `model-pending`). Still model-pending: gender (#89) + lemma (#92) — the live API's `lemma`/morphosyntax modes return degraded surface/empty output and there is no in-session GPU; **atlas-side GPU-prep is now DONE** (pinned revision + npm wiring + `docs/DHARMAMITRA_GPU_RUNBOOK.md`, see Next Steps), so this only awaits a GPU run. Unblock via a healthy API OR a GPU run of pinned `chronbmm/sanskrit5-multitask` (`import-dharmamitra-{morphology,lemma}.py --source local --revision <sha>`, then re-run the `build-*-crosscheck.mjs`) — which also replaces #100's first pass with reproducible junk-free data. *Month 2* — DTB link-target pilot DONE (#102): `build-citation-link-pilot.mjs` turned MW's 15,916 RV `<ls>` citations into **3,996 RV→VedaWeb verse links** (`citation-link-target` queue). FINDING: explicit-locus citations need NO alignment (just locus→URL), so mitra-aligner is only for the residual (quote-only/vague refs, locus-less sources) per `docs/MITRA_ALIGNER_HANDOFF.md`. *Month 2 link-splitting* DONE (#104): audited all 311,933 MW `<ls>` — only 2 combined (already atomised), no pipeline needed. Month-2 remainders: per-hymn verse-count table; a non-explicit-locus source to exercise mitra-aligner (gated on a digital source corpus). Full programme: `docs/DHARMAMITRA_MONTHLY_PLAN.md`. Each integration = its own PR off `main` via `git worktree`.

**Human-gated (no code; tracked in `docs/USE_CASE_PAGE_ROADMAP.md` GTD board):**
- H4 source review — vote the 89 rows in `review/h4_review_sheet.html` (regen: `npm run build-review-sheets`); export decisions.json for /decisions-apply.
- H5 maker correction — ✅ closed as no-edit (see queue above); the 9 source-supported rows stay out of correction flow as before.
- Xref source-check — vote the 40 shared-core rows in `review/xref_review_sheet.html`; prefix-controls already auto-resolved.

## 🚧 Current Work-In-Progress (WIP)

(None currently — all major work items progressing or waiting on external dependencies.)

## 🧠 Dev Notes & Hypotheses

- **SEO `<head>` gotcha (2026-07-01):** Observable Framework's `normalizeFrontMatter` (frontMatter.js) WHITELISTS front-matter keys (`title/sidebar/toc/index/keywords/draft/sql/head/header/footer/pager/style/theme`) and silently drops everything else — so a per-page `description:` in an `.md` NEVER reaches the config `head()` function (`data` only carries title+toc). Per-page descriptions therefore live in a `PAGE_DESCRIPTIONS` route→string map in `observablehq.config.js`, keyed by `path`; `data.title` still drives og:title. Also: `observable build` only emits page-referenced files, so `robots.txt`/`sitemap.xml`/`atlas-card.png` are copied into `dist/` by the npm `postbuild` (extended the pre-existing manifest/sw/favicon copier). Regenerate card/sitemap with `python scripts/make_social_card.py` / `make_sitemap.py`.
- MICRO/MACRO research prototypes (RESEARCH_LAYER_ROADMAP §1): **MICRO is now committed** as `scripts/build-micro-entry.mjs` (`npm run build-micro-entry [-- <lemma>]` → `data/lexico/micro-<lemma>.json`; per-lemma 10-feature matrix across 43 dicts; #172). **MACRO was NOT rebuilt** — it is already covered, and better, by the full-corpus `structural-register.json` + `dictionary-coverage.json` `blockPct` (over ALL entries vs the prototype's 3,000 sample). Don't reconstruct `macro_profile.py`. Worked example: H4/Xref reviewer worksheets (#170) are the human surface for the auto-triage-surfaced rows.
- Moving a doc to `docs/archive/` breaks its OWN outbound relative links, not just inbound references to it — a sweep that "repoints references" must also re-prefix the moved file's links (`](X.md)`→`](../X.md)`, `](../data/X)`→`](../../data/X)`). Rule: resolve each link from the OLD location; if it existed there but not from the new one, add one `../`. Genuinely-dead links (target in another repo, e.g. csl-observatory docs) can't be path-fixed — leave them.
- `.ai_state.md` MUST stay git-tracked (journal protocol). An untrack-then-restore can silently revert it to an OLDER copy and drop journaled work (happened #148→`ad3fa7c`). `git check-ignore` skips already-tracked paths by default; use `git ls-files -ci --exclude-standard` to find ignored-but-tracked files.
- ~~The lone `npm test` failure (`vendored sanskrit-util.mjs matches the canonical source`) is BENIGN and LOCAL-ONLY~~ **RESOLVED (2026-07-01 audit):** the drift-guard now passes locally — the sibling `../sanskrit-util` checkout no longer diverges from the vendored copy. If it ever reddens again, the cause is sibling-branch drift (was `feat/deva-to-slp1`), not the atlas; CI skips it (sibling absent).
- Generated site-data (`src/data/dcs/dcs_lemma_summary.json`, `src/data/lexico/sense_divergence.json`) is gitignored + UNtracked (post-#151): `scripts/sync-site-data.mjs` regenerates it from committed `data/` sources in prebuild/predeploy/predev. Don't commit it; run `node scripts/sync-site-data.mjs` if a local build needs it.
- Deterministic builders set `generatedAt` via `generatedAtForPayload(readJsonIfExists(OUT, fs), payload)` (dataset-meta), NOT `new Date()`, or rebuilds churn a one-line diff. Networked import/crosscheck scripts are the intentional exception. GitHub write-API (merge/PR-create/branch-delete via GraphQL+REST) was TLS-flaky this session — retry idempotently; reads/`git push` were fine. ALWAYS verify a destructive remote op (delete/merge) against `git ls-remote --heads origin`, not the API response — a swallowed timeout reads as a false success.
- Branch hygiene: the repo has `delete_branch_on_merge=true`, so MERGED PR branches auto-delete — never a manual step (don't surface it as a task). Only STALE UNMERGED branches need explicit cleanup: deleted `fix/retrack-ai-state` (its "re-track .ai_state" intent was already done by `ad3fa7c`; its 151-line `2026-06-16` snapshot was stale, superseded by the live journal — merging it would have reverted this session's journaling). Remaining non-main branch `codex/practice-checks-atlas` is another contributor's — leave it.
- R2 promotion gate vs reproduction: "parser promotion" = changing the 5 packet decision rules (deferred). Reproducing the archived package with *existing* heuristics is NOT promotion and is the allowed no-human work.
- H1 key finding: `div` mode (PWG/PWK) over-counts in H1 because `<div>` tags are structural (multi-depth), not semantic sense boundaries. Semi-clause proxy gives 1.127 vs archived 1.161 (3% under). The "no temporal inflation" conclusion reproduces regardless.
- BEN Roman numerals `{@I.@}/{@II.@}` are POS-class markers in compound sub-sections, NOT main sense divisions. Don't add them to H1 count.
- H2H3 panel: SHS/YAT use inline `\b\d+\. ` markers (not in `R2_DICTS`); must use `stripMarkup()` (not `cleanText()` with 220-char truncation) before splitting. WIL→SHS overlap 0.906 ≈ 0.82 archived — near-verbatim copy confirmed.
- Per-entry H1 metric confounded by MW headword-splitting (286k entries → many short); `r2_h1_panel.json` fixed-lemma panel removes this. AP90→AP edge useful for testing revision-only (no inflation expected within same family).
- Dharmamitra: model output is **review evidence only**, never a build input (no LLM inference in `npm run build`). ByT5 (gender/segmentation/lemma) needs a GPU — can't run in-session; the chronology TSV (#90) and detect-language SPM (#95) run on **CPU** and produced real data. detect-language is English-vs-Sanskrit only → poor at German and fooled by Sanskrit loanwords + inflected forms, so #95 is a low-precision demonstrator until a German-aware model replaces it (Month 5). Schema-enum additions (`review-report.schema.json`) collide across separate PRs — resolve by **keeping all** values. Large model snapshots are gitignored; commit only the review queue. Deploy (`Build and deploy csl-atlas`) is FileAttachment-sensitive — #93 fixed a pre-existing red by committing two missing page-data inputs.
- Dharmamitra remote API (2026-06-14, #100): the PyPI `dharmamitra-sanskrit-grammar` v0.1.7 is STALE — it sends `input_sentence` (string) but the live `https://dharmamitra.org/api/tagging/` now requires a `texts` list, so the old call 422s; `dharmamitra_infer._run_pypi` was rewritten to POST the live contract directly. The live API is also DEGRADED right now: `lemma` + morphosyntax modes return surface/empty output (so gender/lemma can't be activated remotely), and `unsandhied` works but ~38% of outputs are degenerate (`____`) or junk (`R+/`, `t`). The segmentation importer now filters those to `modelSegments: null` (→ model-pending); `model-splits-fewer` with model=1 (no split) may be a soft failure.
- DTB link-targets (2026-06-14, #102): the *explicit-locus* path needs NO alignment. MW cites the Ṛgveda as `<ls>RV. v, 86, 5</ls>` (=5.86.5), so link-target = parse locus → URL. VedaWeb (`vedaweb.uni-koeln.de/rigveda/view/id/MMHHHVV`, zero-padded; 5.86.5 → `0508605`, resolves 200) is a Cologne-family, locus-addressable RV edition — ideal target. mitra-aligner is therefore only for the residual (quote-only/vague citations, locus-less sources). Watch for malformed citations (`RV. i, 1, 64, 6` = 4 parts) and verse-range (maṇḍala/hymn validated exactly; verse only capped ≤58 — needs a per-hymn count table).

## ✅ Completed (Recent only)

- **OBS-C citation-register artifact + A02 stale-triple fix (2026-07-02, Fable 5 `claude-fable-5`):** the A01 verification pass found the A02/OBS-C kośa *iti* triple (SKD 69,215 / VCP 22,070 / KRM 6,449) non-reproducible. Root causes measured: (a) csl-orig moved (2026-06 dangling-`<ls>` merges, SKD retroflex fix, BOR added → 43→44 dicts); (b) the 2026-06-10 space-or-quote *iti* rule missed markup-adjacent quotatives — KRM wraps Sanskrit in `<s>…</s>`, so ~2/3 of its sūtra-citing `iti` sat after `>` and was invisible. Fixes: [scripts/obs/citation_register_gaps.py](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/obs/citation_register_gaps.py) now uses a Latin-letter word boundary and emits the committed enveloped artifact [data/obs/citation_registers.json](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/data/obs/citation_registers.json) (per-dict entries/`<ls>`/locator/iti + density ranks; envelope via new Python mirror [scripts/lib/dataset_meta.py](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/lib/dataset_meta.py) of `dataset-meta.mjs`; byte-identical re-run verified). Canonical numbers now: 1,245,644 `<ls>` (59.3 % locator-bearing, gap ≈507K); SKD 80,164 (1.88/entry) / VCP 15,619 (0.31) / KRM 12,359 (**6.00**, densest); SKD rank swap tied-last↔2nd of 44. Regenerated [docs/CITATION_REGISTERS.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/CITATION_REGISTERS.md), A02 Tables 1–2 + abstract/§3.3/§4.1/§4.2/§4.4/§7, A01 §3.4 + data note (citation-register anchor now walkable), HYPOTHESIS_INDEX OBS-C row. Siglum-inventory figures (13,021→9,180, 2,166 ≥10×, −0.7 pp established-siglum decrement) are still the 2026-06 siglum pass, marked as such.

- **A02/A07/A08 revision EXECUTED (2026-07-02, Sonnet 5 `claude-sonnet-5`, off the Fable-5 brief; branch `docs/a02-revision-execute-sonnet`):** ran the full ordered checklist in [REVISION_BRIEF_P2_OBS.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/REVISION_BRIEF_P2_OBS.md). **R1 fusion counter (A02):** new read-only [`build-r2-kosa-fusion.mjs`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/build-r2-kosa-fusion.mjs) → `data/lexico/r2_kosa_fusion.json` classifies every SKD/VCP *iti*-unit as authority-terminal/separable/no-authority; verified against the *dharma* exemplars (SKD's synonym run does close in *ity Amaraḥ*, fused) before trusting the aggregate — **the corpus-scale result does NOT reproduce the exemplar's dictionary-level direction** (SKD 53.3% fused vs VCP 77.6% fused, inverted from the exemplar), so §7 was honestly re-scoped from a dictionary-level "two civilisations" claim to a **record-type-dependent** one (short encyclopaedic entry vs discursive Mīmāṃsā commentary), per the brief's own contingency instruction — no classifier tuning to force the expected direction. Also emitted a stratified ~100-row SKD sample → interactive review sheet [`REVIEW_SKD_ITI_ADJUDICATION.html`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/REVIEW_SKD_ITI_ADJUDICATION.html) (vote→download decisions.json; feeds A08 C-M1 too, per the brief's cross-paper synergy rule). **A02 manuscript:** abstract rewritten 550→244 words; §3.1 gloss-scope sentence added; §5 pooled-logistic paragraph compressed + threshold-sweep sentence + H2-moral sentence added (also in §9); §7 rewritten with the corpus count; §8 exemplar bullet replaced with the classifier's own limitations + MW gold-standard cross-reference; 2 survival/decay refs added (Pagel et al. 2007, Petersen et al. 2012), flagged for author page-number verification; [`RESPONSE_TO_REVIEWERS_P2.md`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/RESPONSE_TO_REVIEWERS_P2.md) written from the brief's concede/defend table. **A07 (`paper_redundancy_and_descent.md`):** Table 2 gained the `b_in_a` reverse-containment column + `|A|`/`|B|`/`|A∩B|` denominators (pulled from the already-committed `data/sanhw1_jaccard.csv`, no new computation); added `fold_sensitivity()` to [`headword_multiplicity.py`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/obs/headword_multiplicity.py) (strips a trailing SLP1 anusvāra/visarga before comparing headwords) — independence 42.2%→38.8% (−3.4 pts), reported as a lower-bound sensitivity check, not the headline; filled the secondary-references TODO. **A08 (`paper_citation_registers.md`):** new [`citation_register_gaps.py`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/scripts/obs/citation_register_gaps.py) (reuses `parse_cslorig.py`'s `<ls>` extraction) filled Table 1's BEN/BHS/AP totals (48,603/48,419/62,672) and computed the rank-swap: SKD is 39th of 44 dictionaries by `<ls>` density, 2nd of 44 by *iti*-density; added the pre-digital-expectation comparator sentence (C-M4) and filled the secondary-references TODO. **Guardrails held:** all three fusion/gap scripts are new read-only readers over `../csl-orig/v02`; the R2 checkpoint/drift/promotion seeders and `src/data/review/` were never touched. Delivered via PR off `origin/main` (fresh branch per the branch-hygiene guardrail) with auto-merge.
- **A02/A07/A08 revision-strategy pass (2026-07-02, Fable 5 `claude-fable-5`, PR `docs/fable-s5-a02-revision-brief`):** wrote [docs/articles/REVISION_BRIEF_P2_OBS.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/docs/articles/REVISION_BRIEF_P2_OBS.md) — the executable P2 major-revision plan (revised thesis: two robust findings H1+H3, §7 fusion promoted from exemplar to a NEW corpus-scale SKD/VCP count `r2_kosa_fusion.json`, H2 reframed as an honest null with a methodological moral; concede-vs-defend table → `RESPONSE_TO_REVIEWERS_P2.md`) + one-paragraph OBS-R/OBS-C verdicts (both minor in substance: A07 = reverse-containment col + denominators + fold-sensitivity number, all latent in `headword_multiplicity.py`; A08 = *iti* hand-validation + Table-1 totals + rank-swap number) + the cross-paper synergy rule (ONE SKD annotation pass serves A02-R1 and A08-C-M1). Key input: the 2026-06-13 referee report is stale — #123–#134 already dissolved H2's positive claim (edge concentration, within-edge z=1.80 p=0.07 n.s.); the brief is written against the LIVE draft + `h2ThresholdSensitivity` (naive gap 3–6 pts across 0.10–0.25; controlled OR nominally sig. but edge-driven post-#133). MDF third-profile call (adopt-bounded / defer serializer) posted on [csl-standards#1](https://github.com/sanskrit-lexicon/csl-standards/pull/1).
- **Repo health audit re-run on `main` (2026-07-01/02, read-only + one fix; Fable 5 `claude-fable-5`):** all green — `npm test` **212/212** (notably the `sanskrit-util` sibling drift-guard now PASSES; the sibling checkout caught up, prior "benign local failure" note retired); `validate-review-reports` (12 review queues schema-valid) / `validate-mw-depth` (0 warnings) / `validate-dict-comparison` (dossier 28,512 lemmas, reader lookup 52,934) all pass; production build clean (56 pages, 159 links, exit 0) with the SEO `postbuild` copying `robots.txt`/`sitemap.xml`/`atlas-card.png` into `dist/` from a fresh build; CI on `main` green (Test/CodeQL/deploy); no TODO/FIXME in `scripts/`, no hardcoded secrets, tree clean. **One defect found + fixed:** [README.md](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/README.md) linked the two R2 tool pages with a stale `.html` extension (dead on GitHub) → corrected to [`src/tools/r2-explorer.md`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/r2-explorer.md) + [`src/tools/r2-h1.md`](https://github.com/sanskrit-lexicon/csl-atlas/blob/main/src/tools/r2-h1.md); commit `d0085dd` pushed to `main`, CI green. Live-doc link sweep otherwise clean (the only other hits are `X.md` pattern placeholders in this journal's dev notes).
- **Site SEO + discoverability foundation shipped + live (2026-07-01, PRs #181 + #182 merged, deploy green):** Ported the csl-observatory SEO treatment. Replaced the static `head` string in `observablehq.config.js` with a `head({title,data,path})` function emitting per-page **canonical**, **meta description**, **Open Graph**, and **Twitter `summary_large_image`** for origin `https://sanskrit-lexicon.github.io/csl-atlas/`, while preserving the existing PWA/favicon/stylesheet markup + service-worker registration. **Distinct descriptions for all 55 routes** via a `PAGE_DESCRIPTIONS` route map (Framework whitelists front-matter keys — see Dev Note). Added a 1200×630 social card (`scripts/make_social_card.py` → `src/atlas-card.png`, PIL), `sitemap.xml` (55 urls, `scripts/make_sitemap.py` parses the config page list) + `robots.txt`; extended the existing `postbuild` to copy the three statics into `dist/`. **#182** fixed the homepage title: `src/index.md` front matter `title: csl-atlas` (repo slug) → `Atlas of the Cologne Digital Sanskrit Lexicons`, so `<title>`/`og:title`/`twitter:title` show the full name (Framework dedupes against the identical config title). Live-verified: root + nested canonicals (`/dicts/mw`, `/tools/dictionary-overlap`), per-page descriptions, og:image (HTTP 200), 55-URL sitemap, robots.txt. Build clean (56 pages, 159 links). **⚠️ Branch-hygiene lesson:** the first commit accidentally landed on `codex/research-desk-query-handoff` (open PR #180) because the repo was checked out there; extracted it to `codex/seo-foundation` off main (→ #181) and force-restored PR #180 to its original commit. Always `git branch --show-current` before committing in this repo.
- **Research Desk query handoff (2026-06-29, branch
  `codex/research-desk-query-handoff`):** Added compact Lookup/Dossier handoff
  controls for the five Student Research Desk exercise lemmas (`dharma`,
  `agni`, `gam`, `rAma`, `mokza`). The controls submit GET requests to the
  existing Reader Lookup and Lemma Dossier `?q=` contract without changing
  lookup normalization, generated datasets, service-worker behavior, or
  `csl-orig`. Static `<a>` query links were avoided because Observable's build
  validator treats local search-param links as broken; GET forms preserve direct
  handoff while keeping link validation green. Validation: `git diff --check`
  clean; `npm test` PASS (212/212); `npm run build` PASS (56 pages, 159 links
  validated); 390px manual check on `/research-desk.html` had
  `mainOverflowCount: 0`; direct query loads prefilled
  `/tools/reader-lookup.html?q=dharma` and
  `/tools/dictionary-dossier.html?q=rAma`.
- **Student/researcher entry paths shipped (2026-06-29):** Added
  `src/research-desk.md` as the student-facing one-word workflow and
  `src/researcher-dashboard.md` as the researcher-builder action surface, then
  promoted both under a new `Research paths` nav group and the landing page's
  first-viewport start cards. Follow-up same session: added `src/student-lessons.md`
  to turn lookup/dossier/citation evidence into short classroom exercises, and
  `src/evidence-bridges.md` to route WhitneyRoots, VisualDCS, SanskritSpellCheck,
  MWS, CommentaryStrategies, csl-guides, and csl-standards as link/chip/contract
  candidates without importing external data. To restore validation after the
  page work, refreshed the atlas-owned H5/H4/Xref source-check packets and
  worksheets from their existing generators against the current local
  `csl-orig` snapshot; this updated source anchors/excerpts only and did not
  touch cross-repo files. Validation: `npm run build` PASS (56 pages, 159
  links; new routes rendered, including `/research-desk` and
  `/researcher-dashboard`); `npm test` PASS (212/212).

**Etymology / derivation oracle — DONE (2026-06-26, branch `feat/etymology-oracle`):** `scripts/build-etymology-oracle.mjs` (npm `build-etymology-oracle`) **consumes** csl-orig's pre-built derivation layer instead of the atlas re-extracting: per-dict summaries from the 10 `<dict>_etymology.tsv` (rows, root coverage, top roots, `rootStyle` = root_slp1 / source_slp1 / iast-root), plus the cross-dict **root agreement** + **productivity** tables read **verbatim** from `csl-orig/v02/etymology_stats/` (NOT recomputed — a naive exact-match metric conflates citation-form variants + kāraka senses and would contradict the established Wilson-CI finding). Output: new `src/data/etymology-oracle.json` (+ `data/` mirror); 10 dicts, 64,896 rooted headwords; agreement reproduces Apte~AP 93.6% / PWG~PW 93.1% top, WIL~MW 8.4% (Wilson outlier). Read-only: no reviewed packet / checkpoint / overlay touched; not wired into the build. Realizes the PROJECT_INTERLINKS "etymology.tsv → csl-atlas" feed (P4/A04 derivation cluster). ➡️ Optional next: a page surfacing the oracle.

**P5 "Pointing Inward" cross-reference-lineage paper drafted — DONE (2026-06-16, PR #143):** `docs/articles/paper_xref_lineage.md` (~2,800 words) — the cross-reference companion (P5). NB: P5 had NO file or topic committed (only P2/P3/P4/P6 were); author confirmed P5 = the XREF-CORE paper. Thesis: a dictionary's internal cross-reference graph (`cf.`/`Vgl.` pointers) is a measurable descent signal, but only once calibrated by a positive control and cleaned of convention hubs. Built on the committed xref lineage layer + hub-review packet. Centrepiece = a **positive control**: AP×AP90 (same dictionary, two Apte editions) overlap 85.5%/84.7%, Jaccard 0.74 (the descent ceiling); against it, MW×PWG (`cf.` vs `Vgl.`) reaches only 21.8%/9.1% (J=0.069) on the 2,538 shared-source lemmas (641 identical edges) → a SHARED CORE (variant/cognate pointers both record), not wholesale inheritance; PWG ~3× denser (22,937 vs 7,637 edges). Two series-ties: prefix-convention hubs (PWG → a° 320, mahā° 254) are a convention artifact (P3's convention axis) held out of the lexical-core reading; Benfey does ZERO internal Sanskrit cross-referencing — a content fact, the cross-ref analogue of P4's structural zero. All pair-table values/rates/Jaccards/hub counts verified against `xref_hub_review.json`. Honest limits (floor-not-ceiling, directed edges, density asymmetry, single positive control, machine labels). Registered in PUBLICATIONS.md (P5 row). Docs-only; graph/stemmatology refs + byline flagged.

**P3 "Three Axes of Descent" methods paper drafted — DONE (2026-06-16, PR #141):** `docs/articles/paper_three_axes_descent.md` (~3,000 words) — the methodological companion (P3) to P2/P4/P6. NB: P3 had NO file or topic committed; only P2/P4/P6 were defined, so I asked the author, who confirmed P3 = the THREE-AXES methods paper. Thesis: dictionary inheritance is a VECTOR not a scalar — content (directed headword containment), convention (L0 house-style fingerprint + phylogenetic bootstrap), and microstructure (H6 citation/grammar register + M1–M5 layers) are separable axes a single descent score conflates. Built entirely on the committed `data/lexico/three_axis_comparison.json` (13 documented L0 edges). Centrepiece is a **double dissociation on one parent**: from PWG, PWG→MW = content 0.89 / convention 0.37 / bootstrap 0.013 (content without convention; MW holds 94,776 of PWG's 106,083 lemmas = 0.89, but only 0.49 of MW's 194,084), while PWG→Schmidt = content 0.07 / convention 0.68 / bootstrap 0.68 (convention without content). All table values, directed-containment figures and band counts verified against the packet. §5 ties the discipline to the empirical companions (P2 family-confound, P4 content-without-convention, P6 no-European-microstructure); limitations stated plainly (containment is a size-confounded floor not proof of copying; readings are machine prompts not adjudicated lineage; 13 European-skewed edges). Registered in PUBLICATIONS.md (P3 row). Docs-only; references reuse the verified lexicography works, stemmatics/phylogenetics refs + byline flagged for the author.

**R2 hypothesis-rigor arc — DONE (2026-06-15/16, PRs #123/#125/#126/#127/#129/#130/#132/#133/#134):** nine isolated PRs off `origin/main` hardening the R2 sense-inheritance hypotheses, then surfacing, adjudicating, applying, and writing them up; discipline held throughout — model/heuristic output is review evidence only, parser-rule changes go through R2 checkpoint review (never silently applied), deploy verified green after each merge. **#123 H2** confound test: enriched per-sense rows (cited, survived, overlap, position, glossLen, crossDict) + from-scratch JS logistic IRLS (`fitLogistic`) + CR1 cluster-robust sandwich (`clusterRobustSE`) + edge fixed effects; the naive cited-survival gap (z=3.0 p≈0.002) attenuates to OR 1.75 **p=0.16** once centrality-controlled + lemma-clustered + edge-FE → pseudoreplication demonstrated. (Mid-impl fix: a spurious negative crossDict coef appeared without edge FE — the 3 edges have 90% vs 7% survival baselines; adding edge dummies drove crossDict to ~0, p=0.67.) **#125 H3R** artifact: `verify-yat-sense-artifact.mjs` proves the wil→yat "condensation" is the inline-number splitter misreading YAT's semicolon-packed meanings (inline 1.0 vs true 5.71/lemma, 75% class-number markers) → exemplar retracted, edge flagged `senseExtractionArtifact`. **#126** packaged the fix as a reviewer-gated 26-row checkpoint packet (`needs-human-review`, empty decision fields; YAT→apply, SHS control→keep inline) — NOT a silent parser change. **#127 H1R** within-family controls: the bivariate r≈0 is underidentified (year = 11 dict-points / ~7 families); within-family deltas show ALL four multi-dict families DECLINE (mean −0.11 units/lemma·yr), the family-controlled OLS year slope is −0.051/yr CI [−0.186,+0.084] (near-saturated → bounds, not a measured null), and western-only r=−0.11 (lexicographic, not parser artifact). Additive everywhere (h2/h3r/h1-panel values byte-unchanged; only new `*Controlled` blocks + sidecars). **#129 H2 threshold-sensitivity** (referee M4): the design matrix is threshold-independent, so refitting #123's logistic at cutoffs {0.10,0.125,0.15,0.175,0.20,0.25} is exact & cheap (h2Rows carry raw overlap) — naive gap persists but controlled cited effect stays non-significant (OR 1.44–1.75, p 0.16–0.35) across the grid → robust; perfect-separation guard (`fitOk`); also put #123's controlled result on the r2-h2h3 page for the first time (hand-authored Robustness section outside the R2-GEN blocks) and the page-regen incidentally synced the stale H3R "drastic condensation" row to #125's committed "parser artifact" flag. **#130 P2 manuscript reconciliation** (docs-only): the paper still printed naive results; downgraded H2 (answers referee M2), retracted the Yates condensation exemplar, strengthened H1 to contradicted-in-direction; each claim now points to its committed evidence block. **#132** adjudicated the #126 packet (25 promote / 1 retain `avazwabDa`; reviewer gasyoun) with evidence notes + hardened the generator to preserve human decisions by checkpointId (idempotent). **#133** APPLIED the YAT semicolon counter (`splitSemicolon` + lone-adjective refinement): H3R wil→yat = genuine ~9→5.7 condensation, artifact resolved; recompute exposed H2 cited-sense concentration (82/84 on ap90→ap) → pooled OR edge-unstable, clean within-edge test z=1.80 p=0.07 (not significant), H2 conclusion unchanged; added `withinPrimaryEdge`/`citedByEdge`; 2 new `splitSemicolon` tests. **#134** re-synced the manuscript to #133 (Yates un-withdrawn as real condensation — 3 sound edges; H2 edge-concentration framing; uncited 0.591→0.705). **#137** filled the P2 secondary references (standard lexicography/Indology works + companion papers + inline anchors), replacing the `[TODO]` block. **#139** verified the reference pagination against the de Gruyter HSK 5.1 contents and corrected Wiegand 1989 from 409–462 to 409–461 (the next article "Arten von Mikrostrukturen" occupies 462–501). Tests stayed green throughout (the recurring `sanskrit-util` drift-guard failure is unrelated sibling-checkout drift from PR #119). The arc is now closed; only author-only P2 tails remain (byline/venue, title).

**Broad dictionary analysis parity + Reader/Dossier lookup polish — DONE (2026-06-14, PR #106):** Broad neutral coverage/overlap now uses 40 eligible Sanskrit/BHS dictionaries while Core 7 remains stable for legacy/deep comparison. Deep analysis is adapter-gated: grammar/POS has 14 supported dictionaries, citations have 13 supported `<ls>` dictionaries plus SKD/VCP/KRM partial `iti` diagnostics and WIL weak `iti` diagnostics, homonyms have 20 supported dictionaries, and senses have 14 supported dictionaries; missing markup is unavailable, never zero evidence. Reader Lookup and Dictionary Dossier keep public display IAST-only, add `?q=` plus `scope`/`mode` deep-link support, share lookup helpers, and load broad shards on demand. Source links are trailing-slash-safe and local-only dictionaries never emit broken GitHub hrefs. Generated data builders preserve `generatedAt` on content-identical rebuilds to avoid timestamp churn. Verification: PR #106 green (`Test`, `CodeQL`), squash merged to `main` as `3c4646e`, and post-merge `Test`, `CodeQL`, and `Build and deploy csl-atlas` all succeeded. Local checks before merge included broad/headword builders, dictionary builders, capability audit, citation/sense builders, validators, `npm test` (184 tests), and temp Observable build (51 pages, 130 links).

**Dharmamitra Month 2 — DTB link-splitting audit — DONE (2026-06-14, PR #104):** Second DTB sub-task, and it's a near-empty problem. `scripts/build-citation-link-split.mjs` audited all **311,933** MW `<ls>` citations for combined references (semicolon / digit-range / open sequence) → only **2** combined (`Bālar. iv, 11; x.` semicolon; `PadmaP., Svargakh. 1-5.` range, =6.4e-6 of corpus). The Cologne digitizers already atomised citations into one `<ls>` per reference, so link-splitting needs NO pipeline; both exceptions queued (`citation-link-target` queue, split correctly — siglum re-attached, range expanded). No schema change (reuses #102's queue + `citation` kind). PWG has ~4 more semicolon refs (noted). validate-review-reports green.

**Dharmamitra Month 2 — DTB link-target pilot — DONE (2026-06-14, PR #102):** First Dictionary-to-Book artifact in the repo. `scripts/build-citation-link-pilot.mjs` parses MW's **15,916 Ṛgveda `<ls>` citations** and emits **3,996 distinct verse links** to VedaWeb (`vedaweb.uni-koeln.de`, zero-padded `MMHHHVV` stanza id; sampled link resolves 200) as the `citation-link-target` review queue. Maṇḍala/hymn ranges validated against RV structure; verse capped ≤58 (caught malformed 4-part `RV. i, 1, 64, 6`). Breakdown of 15,916: 5,738 explicit-verse → 3,996 loci; 9,707 bare `RV.` (work-level); 354 hymn-level; 79 maṇḍala-only; ~38 malformed/out-of-range. **Reframing:** explicit-locus citations need NO alignment — mitra-aligner is for the residual (quote-only/vague refs, locus-less sources). Atlas only proposes; human ratifies before any write to csl-orig. Schema + `citation` kind added; validate-review-reports + deploy green. Remainders: per-hymn verse table, link-splitting, a non-explicit-locus source.

**Dharmamitra Month 1 — compound-depth activated — DONE (2026-06-14, PR #100):** First ByT5 pass for the compound-depth queue. No in-session GPU, so used the remote API and hit two things: (1) the PyPI package is stale (live API moved `input_sentence`→`texts` list, 422) → rewrote `dharmamitra_infer._run_pypi` to call the live endpoint; (2) the live API is degraded — `lemma`/morphosyntax return surface/empty (gender #89 + lemma #92 stay blocked), `unsandhied` works but ~38% junk. Added a junk filter (degenerate/`R+/`/single-char → `modelSegments: null` = model-pending). Result: `compound-depth-crosscheck` populated with **2,173 real verdicts of 3,496** (540 agree, 1,439 model-splits-fewer, 194 model-splits-more; 1,323 pending). Snapshot `src/data/external/dharmamitra-segmentation.json` committed (662 KB). Trust block flags the degraded-API first pass + the model=1 soft-failure caveat; recommends a healthy-API / GPU re-run for reproducible junk-free data. Deploy + tests green.

**Dharmamitra integration layer — DONE (2026-06-14, branches `feat/dharmamitra-*`; PRs #89–#96 merged to main):** A review-evidence layer cross-checking the deterministic atlas against [dharmamitra](https://github.com/dharmamitra) NLP models — all two-step (networked `import-*` → `src/data/external/` snapshot; deterministic `build-*-review.mjs` → schema-conforming queue via `review-report.mjs`). **#89** gender cross-check — ByT5 `chronbmm/sanskrit5-multitask` morphosyntax as an independent third vote over the 4,556 cross-dict gender conflicts (scaffold, model-pending) + vendored `scripts/sanskrit_tags.tsv` (offline tag expansion) + `docs/DHARMAMITRA_INTEGRATION.md`. **#90** source-layer anchoring — reinstated `import-dharmamitra-chronology.mjs` (sanskrit-dating); **REAL data**: 1,618 dated works / 1,283 anchors → per-layer date bands (vedic ≈1125–567 BCE … lexicographic ≈1200–1892 CE) via an explicit layer↔era crosswalk; the one non-pending queue. **#91** compound-depth — ByT5 `unsandhied` vs markup `compoundSegmentCount` over 3,496 deep compounds (model-pending). **#92** lemma-normalization — ByT5 `lemma` vs `normalizeSlp1Lemma` over the 1,913 all-7-dict lemmas (model-pending) + NEW shared `scripts/lib/dharmamitra_infer.py`. **#93** deploy fix — committed two long-missing FileAttachment inputs (`src/data/dcs/dcs_lemma_summary.json` + `src/data/lexico/sense_divergence.json`) that had failed Build-and-deploy since before this work; verified clean `npm install && npm run build`. **#94** refactored #89/#91 importers onto the shared module (3 SLP1→IAST/HF copies → 1). **#95** PWG markup check — detect-language eng/skt SentencePiece (CPU, **REAL data**) over 183k `{#...#}` Sanskrit spans → 1,449 flags of foreign-looking content; **low-precision demonstrator** (inflected Sanskrit forms misfire; the German-gloss `{%...%}` direction was tried and abandoned at ~95% FP) — needs a German-aware model. **#96** `docs/DHARMAMITRA_MONTHLY_PLAN.md` (6-month programme) + `docs/MITRA_ALIGNER_HANDOFF.md` (DTB link-target spec). Deploy green throughout. Done in isolated worktrees off `origin/main`; the concurrent r12 microstructure branch + its uncommitted user WIP never touched.

**P2 referee-proofing + stat rigor — DONE (2026-06-13, branch docs/referee-p2):** Adversarial pre-submission referee report on paper_sense_inheritance.md → `docs/articles/REFEREE_P2_sense_inheritance.md` (Lexicographica-referee voice, recommendation = major revision, M1–M7 + minors, applied-vs-pending table). Applied the safe fixes IN the manuscript: computed + added real stats — H2 survival 0.762 [0.661,0.840] vs 0.591 [0.554,0.626], two-proportion z=3.0 p≈0.002 χ²=9.3 (was just "17-point gap"); H1 non-significance (r=0.036 t=0.11 df=9; panel r=0.093) framed as the intended null; abstract now says "significant (p≈0.002)"; softened §7 "first corpus-scale demonstration" (rested on dharma alone, n=1) to exemplar + kośa-parser-scaling path; added 2 limitations (single-lemma fusion; hand-chosen 28-noun panel). Pending AUTHOR items flagged: M2 citation-vs-centrality confound (sharpest), M4 threshold-sensitivity + English-gloss scope, M6 cross-ref the MW gold standard, references [TODO]. Lane 1 of 4 (referee-proof drafts); next: Redundancy & Descent + Two Citation Registers, then draft P3+P4, then kośa macrostructure, then Whitney crosswalk for the learner layer.

**Learner's reading layer v1 — DONE (2026-06-13, branch feat/learner-reading-layer):** The Q4 student-facing roadmap item. `scripts/build-learner-index.mjs` (npm run build-learner-index) joins lemma-lookup.json (52,934 lemmas, dict coverage + grammar-reliable gender) with the DCS frequency band (data/dcs/dcs_lemma_summary.json) → `src/data/learner/learner-index.json` (28,538 with a band; by band 5:640 4:3095 3:7880 2:9976 1:6947 0:24396). New Observable page `src/tools/learner-reading-layer.md` (Reader-mode nav): frequency = study priority (band 5 = learn first), per-lemma card = freq band chip + dict coverage (grammar-reliable highlighted) + gender + source link; search (SLP1/IAST) + browse-by-band. Bilingual EN/RU (locales). Trust block is honest: bands coarse, band 0 ≠ unused, NO Whitney/gaṇa join (not in csl-atlas — deferred), survival-senses only a 28-lemma panel. 159 tests pass; locale-key contract + dataset-field contract checked statically. NB: Observable CLI not installed in this worktree → could not run dev-server/build render; verified via tests + static contract + mirrors reader-lookup.md idiom exactly.

**FAIR citation metadata (sprint slice 1) — DONE (2026-06-13, branch feat/fair-citation-metadata):** Addresses review gap G1 (nothing citable). Added `CITATION.cff` (CFF 1.2.0; authors Gasūns/Funderburk/Andhrabharati, CC-BY-SA-4.0, csl-orig as a referenced data source), `.zenodo.json` (pre-stages a software deposit so a tagged release auto-mints a DOI once Zenodo is connected), and a README "How to cite" section (provisional citation + dataset-level provenance pointer + evidence-label pointer). YAML/JSON both parse + structural asserts pass; cffconvert not installed locally (GitHub validates CFF on render). Boundary-clean: PROV-O/TEI evidence-label crosswalk (gap G3) deliberately deferred to csl-standards (standards mapping is not csl-atlas's domain). Pending AUTHOR actions flagged in README: register ORCIDs + add them; confirm Andhrabharati's legal name; connect Zenodo + cut a release for the first DOI.

**Per-dataset licence fields (FAIR G6, slice 2) — DONE (2026-06-13, branch feat/envelope-license-dicts):** Extended the licence to the reader-facing Comparative Dictionary Lab + structure datasets. 6 builders now spread `licenseFields()` after schemaVersion: build-dictionary-comparison (its envelope() → 10 src/data/dicts files), build-citation-apparatus, build-sense-depth, build-semantic-fields, build-structural-register, build-xref-lineage. All 15 src/data/dicts/*.json licensed IN PLACE (one-shot script, deleted) — NOT regenerated, because regenerating surfaced PRE-EXISTING STALENESS: structural-register.json rows are stale vs microstructure_fingerprint.json (xrefEdges 0→196, rootEntries 0→1643, dominantLayer ""→"root"), which would cascade to the three-axis packet/test. That data refresh is a separate content change — flagged as a task chip (task_79faa517), kept OUT of this licence PR. Diffs verified licence-only; 159 tests pass; validate-dict-comparison + validate-review-reports pass. Still NOT covered (slice 3, lower priority): R2-chain intermediates + internal packets in data/lexico/ (build-order-coupled; defer).

**Per-dataset licence fields (FAIR G6, slice 1) — DONE (2026-06-13, branch feat/envelope-license):** New `scripts/lib/dataset-meta.mjs` = single source of truth (`DATASET_LICENSE="CC-BY-SA-4.0"`, `licenseUrl`, `licenseFields()`); CC-BY-SA-4.0 matches repo LICENSE + package.json + csl-orig source, so derived data inherits cleanly (no exceptions). Threaded into the two SHARED envelope helpers: `reviewPayload()` (→ all review-queue datasets) and the MW depth `envelope()`. `license` is now required in `review-report.schema.json`; ARCHITECTURE envelope list updated; maps to `dct:license` per the csl-standards crosswalk. Existing 6 review files migrated IN PLACE (one-shot script, deleted) — NOT via builders, since `build-r2-checkpoint-review` re-seeds empty and would wipe the 10 decisions; verified items byte-identical (r2-checkpoint diff = +2 license lines only; 4556+130+7+10+151+449 items preserved). MW depth regenerated. 159 tests pass; validate-review-reports + validate-mw-depth pass.

**P2 §3.3 three-artifact strengthening — DONE (2026-06-13, branch docs/p2-three-artifact-finding):** Folded the PWG + BEN window findings into `paper_sense_inheritance.md` §3.3. The experiment is now framed in three outcome classes — exact untuned reproduction (rama:ben 7=7, dharma:vcp 9=9, control gam:ap 16=16), "the legacy count is the artifact" (PWG div-depth, BEN root+participle cut, with the source-faithful window verifiable against the printed page), and the SKD register limit. New **Table 2** (source window vs legacy count + what the legacy count actually was: PWG gam 7/30, dharma 11/5; BEN gam 9/23; SKD dharma 1/4); §6 inheritance table bumped to Table 3; abstract method sentence strengthened; "legacy"≈"archived" terminology bridged once. Reframes the contribution: not "reproduces the legacy counts" but "demonstrably more faithful to the print than the counts it is checked against." Branch off post-merge-train main (4a36ea2).

**BEN run-scope window — DONE (2026-06-13, branch feat/ben-run-window):** Source probe of Benfey gam (L4326, 172 sense rows) revealed it is ONE record holding 42 numbered marker-runs: run 0 = 9 bare finite-root senses ("To go/move…"); runs 1–3 = primary derivatives (participles gata/gamya, causative gamaya — 24 senses); runs 4–41 = preverb-combined lexemes (each "-- With <preverb>", 138 senses). The archived 23 = runs 0+1 = root + the gata participle only — an arbitrary cut mid-derivative, i.e. the archive is the artifact (same finding shape as PWG). Kept the faithful bare-root window (9, ratio 0.391) and refined `markerRunWindow` to label the side evidence precisely: `bare-root-run` / `primary-derivative-run` / `preverb-lexeme-run`. New `benPreverbBoundary()` (source-read) + pure `benBoundaryFromParts()` detect the preverb zone via the "-- With <preverb>" cue (capital-W + "--" discriminates from lowercase "(with acc.)" glosses); boundary = run 4 for gam. 155 tests pass (markerRunWindow test rewritten + benBoundaryFromParts hermetic test). NB: did NOT widen to match archive — archive parity is not the optimization target. Three high-drift promoted rows now all explained as ARCHIVE artifacts, not parser gaps: PWG (div depth ≠ sense), SKD (sense+citation fused), BEN (root + one participle = arbitrary cut).

**PWG sense-enumeration window — DONE (2026-06-12, branch feat/pwg-sense-window):** `divSourceWindow()` in `scripts/build-r2-promotion-experiment.mjs` now follows the PRINT's own enumeration — numbered top-level divisions ("— N)" at div n="1") of the target-primary record, before the first div n="p" preverb block; `<div n>` depth values are never counted as senses. Probe confirmed: ¹gam = exactly 7 numbered senses + 66 preverb blocks; dharma = exactly 11 numbered senses + a)/b) subdivisions. New experiment ratios: gam:pwg 11.77 → 0.233 (window 7 vs archive 30), dharma:pwg 4.20 → 2.20 (window 11 vs archive 5). Interpretation: the residual flipped sides — the ARCHIVE was the artifact (counted depth segments + preverbs for gam; saw only "3 divisions" for dharma where the print enumerates 11). Window counts are now verifiable against the printed dictionary. 155 tests pass (divSourceWindow tests rewritten for enumeration semantics).

**P2 manuscript drafted — DONE (2026-06-12, branch docs/paper-sense-inheritance):** `docs/articles/paper_sense_inheritance.md` — *Condensation, Not Inflation: Sense Inheritance in the Sanskrit Dictionary Family, 1822–1957* (Lexicographica target, IJL alternate). Empirical core: H1R flat year-trend (r=0.036; panel r=0.093, family means table), H2 citation-survival 0.762 vs 0.591 (n=84/723, threshold 0.15), H3R three edges (WIL→SHS overlap 0.906 copy; WIL→YAT drift −8 condensation; AP90→AP −3.07 revision), §7 = the SKD sense/citation-fusion finding from the promotion experiment (paryāya run ends in ity Amaraḥ → sense and authority are one iti-unit; VCP separates cleanly 9=9). All numbers from live artifacts, not the 2026-05-31 figures. Registered in `docs/PUBLICATIONS.md`. Open author decisions: secondary references ([TODO] block), byline, venue confirmation.

**R2 parser-promotion experiment — DONE (2026-06-12, branch feat/r2-promotion-experiment):** `npm run build-r2-promotion-experiment` (new `scripts/build-r2-promotion-experiment.mjs`) applies the 7 promoted checkpoint window rules to the source-backed anchor rows; outputs `data/lexico/r2_promotion_experiment.json` + generated `docs/R2_PROMOTION_EXPERIMENT.md`. Additive only — no existing R2 output changed; non-window rows retained as labeled side evidence. Results: `rama:ben` window = archive exactly (7=7, ratio 1.0); `dharma:vcp` definition-iti-unit window = archive exactly (9=9); `gam:ap` parity control held (16=16); `dharma:ae` high band 18 vs archive 24 (0.75); `dharma:skd` over-classifies authority (1 vs 4 — kośa paryāya lists end in ityamaraH, so sense-bearing units get authority labels; definition/authority NOT disjoint in SKD); `gam:ben` run-0 window 9 vs archive 23 (archive evidently spanned more than the first numbered run); `gam:pwg` residual 6.2 ratio = PWG `<div n>` depth-inflation (known: div tags are structural, not sense boundaries). Tests: 154 pass (7 new in `test/promotion-experiment.test.mjs`); stale pre-decision assertions in `test/orchestrators.test.mjs` upgraded to decision-integrity checks. Known follow-up: `build-r2-drift-explanation` still hard-requires empty human fields and would now throw if rerun — needs a post-decision mode.

**R2 checkpoint review — DONE (2026-06-12):** All 10 `r2-checkpoint` rows decided in `src/data/review/r2-checkpoint-review.json` (reviewer: gasyoun, interactive source-verified review against local csl-orig). Outcome: 7× `promote-parser-candidate` (PWG gam/dharma supplements+homonym, BEN reset-runs + rama/rAma bundle split, AE nominal banding, VCP/SKD iti-unit taxonomy), 2× `retain-side-evidence` (AE verbal reverse rows = collocational context; AP iti nested-marker gap), 1× `control-only` (AP gam exact parity, nested-marker-gap dropped). Key asymmetry recorded: AE reverse bands promote for nominal lemmas only — verbal lemmas stay context evidence. `npm run validate-review-reports` passes (10 reviewed-ok). Parser promotion experiments are now unblocked; P2 (sense-inheritance paper) no longer review-gated.

**OBS-R / OBS-C corpus-evidence layer — DONE (2026-06-10, merged to main; separate cross-repo effort):** Corpus-wide (43-dict) quantifications framed as extensions of existing Type-1 hypotheses (not parallel claims), routed through `docs/HYPOTHESIS_INDEX.md`.
- **PR #53** — integrated OBS-R + OBS-C: `docs/CORPUS_REDUNDANCY_GENEALOGY.md` (1.49M entries → 409,649 distinct lemmas = 3.65:1; 57.9% redundant; structured novelty; MW-absorber stemma via containment + pub-year) and `docs/CITATION_REGISTERS.md` (two registers — `<ls>` 1.23M, 59% resolvable vs indigenous `iti` SKD 69k/VCP 22k/KRM 6.4k at 0 `<ls>`; quantifies `INDIG-CITE`). Added `OBS-R`/`OBS-C` rows to HYPOTHESIS_INDEX.
- **PR #54** — `scripts/obs/headword_multiplicity.py` (format-aware: fixes the abch/acph/acsj `<syns>` kośa parser gap → all 43 dicts; kośas re-group attested words, abch 3.3% unique) + `scripts/obs/siglum_families.py` (abbreviation-family **candidate generator** — review worklist, not auto-merge).
- **PR #55** — adjudicated siglum families into `src/data/dict-source-aliases.json` (seed → 27 canonical works, 138 aliases) + `docs/SIGLUM_ALIAS_ADJUDICATION.md`; verified via `canonicalSiglum()` (true variants merge; distinct works kept apart, e.g. Rājataraṅgiṇī ≠ Rājanighaṇṭu, Kathāsaritsāgara ≠ Kaṭha-Upaniṣad). Long tail stays in the queue by design.
- **PR #66** — `.gitignore` `src/*.pdf` (scoped; tracked `docs/refs/` PDFs unaffected).
- **Manuscripts (2026-06-11, PRs #68/#69 merged):** `docs/articles/paper_redundancy_and_descent.md` (OBS-R — *Redundancy and Descent*; abstract+7 sections+2 tables) and `docs/articles/paper_citation_registers.md` (OBS-C — *Two Citation Registers*). Both target the *International Journal of Lexicography*; registered in `docs/PUBLICATIONS.md`. Open author decisions: venue confirmation, secondary references (`[TODO]`), byline.
- Cross-repo: org-process twin **OBS-Q** lives in `csl-observatory` (PRs #31/#32: correction-sustainability finding + generator; writeup not yet drafted — different venue). Provenance memo: `csl-observatory/reports/obs_rc_atlas_bridge.md`. Done in isolated worktrees off `origin/main`; the concurrent microstructure/R2 branch was never touched.

**Documentation updates — DONE (2026-06-11):**
- CHANGELOG.md: Added 2026-06-10 entry covering R2 page regeneration + deferred engineering + tests.
- R2_PAGE_REGEN_SPEC.md: Marked COMPLETED; updated status to reflect live data-driven pages.

**MICROSTRUCTURE_DECISIONS round 12 — DONE (2026-06-10, docs/microstructure-decisions-r12):** Round 12 appended to `docs/MICROSTRUCTURE_DECISIONS.md` documenting KRM cluster parse: anubandha slot empty for all 1,757 KRM roots; parenthesised cluster with bare short forms (`para`/`A`/`uBa`, `saka`/`sa`/`aka`); pada 170→1,378, trans 50→1,735.

**Deferred engineering — DONE (2026-06-10, feature/deferred-engineering):**
- H1 panel viz refresh: `r2-h1.md` now shows generated full-corpus scatter + new 30-lemma deconfounded panel scatter (`h1PanelPoints` in `build-r2-pages.mjs`). Archived static SVG removed. Trust Block + caption updated to live-data language. 2 new tests → 147 total.
- Sense-divergence map: `data/lexico/sense_divergence.json` (6,777 lemmas; 1,282 with range > 0), `src/tools/sense-divergence.md` Observable Framework page with filterable ranked table, `scripts/lexico/build_divergence_map.py` generator. Added to Review queues nav. `npm run build-divergence-map` script.

**VisualDCS adapter — DONE (merged to main):** `dcs_lemma_summary.json` (83,273 lemmas, log10 bands), `scripts/lib/dcs-summary.mjs` loader, DCS band chip on Lemma dossier page. 145 tests.

**R2 page regeneration — DONE (2026-06-10, feature/r2-page-regen):**
- `scripts/build-r2-pages.mjs` (`npm run build-r2-pages`): pure SVG generator helpers (yearToX, rateToY, patternColor, h1Points, h2Bars, h3rDumbbells) with marker-based file injection (idempotent `<!-- R2-GEN:START/END -->`).
- H1 scatter: 720×440 SVG from `r2_h1.json.rows`, family legend, Pearson-r caption.
- H2 bars: 720×300 SVG with 2 groups (Cited #2ca02c, Uncited #888), 4 bars (restored+archived per group), gap annotation.
- H3R dumbbell: 720×300 SVG with 3 rows (wil→shs, wil→yat, ap90→ap), dumbbell marks, arrowheads, overlap labels, archived ghost circles, pattern colors (copy #2ca02c, condens #d62728, revision #ff7f0e).
- New page `src/tools/r2-h2h3.md`: full layout with `<style>`, `<h1>`, intro `.note`, Trust Block, Chart A, Chart B, data table, finding paragraph. All numbers from `r2_h2h3.json` (H2 0.762/0.591, H3R overlaps 0.906/1.0, drifts).
- 5 new tests (`patternColor`, `yearToX`, `rateToY`, `h1Points`, `h2Bars`, `h3rDumbbells`) → 140 total, all pass.
- Navigation: r2-h2h3 added to `observablehq.config.js` alongside r2-h1/explorer.
- Acceptance: ✅ all three pages regenerate, ✅ idempotent (git diff empty on rerun), ✅ 140 tests green.
- Ready for PR; boundary: presentation only (no parser/JSON-schema changes).

**VisualDCS consumption contract DONE (2026-06-09, PR #52 merged to main).** `docs/VISUALDCS_CONSUMPTION_CONTRACT.md` specifies `dcs_lemma_summary.json` (SLP1-headword-keyed, band-level freq 1–5, optional formCount + era), join key (normalizeLemma; Patel variants resolved atlas-side), ≤10 MB budget, out-of-scope list, optional read-only adapter plan, 5 acceptance gates. Cross-referenced from BOUNDARY_RULES.md + RESEARCH_LAYER_ROADMAP.md §7. Ball is in VisualDCS's court: it must emit the file before the atlas wires the adapter. Until then, only an external hyperlink to VisualDCS is in-bounds.

**R2 restore — H2/H3R slice (2026-06-09, main):**
- `build-r2-h2h3.mjs` (`npm run build-r2-h2h3`): restores `r2_h2h3.json`. Reconstructed 28-noun panel from nouns in WIL/SHS/YAT/AP90/AP with 3–9 WIL explicit senses. SHS/YAT split via inline `\b\d+\. ` (`splitInlineNumber`) using `stripMarkup` (avoids 220-char `cleanText` truncation). Gloss overlap: word-level Jaccard, stop-words filtered.
- H2 SUPPORTED: cited survival 0.762 (n=84) vs uncited 0.591 (n=723), gap=0.17. Archived: 0.70/0.54. Direction and magnitude reproduce.
- H3R: WIL→SHS overlap 0.906 (near-verbatim copy, archived 0.82); WIL→YAT des=1.0 (drastic condensation, archived 1.1); AP90→AP drift -3.1 (revision, archived -4.5). All three patterns confirmed.
- 9 new tests (`splitInlineNumber`, `glossOverlap`) → 130 total, all pass.
- `R2_REBUILD_CONTRACT.md`: `r2_h2h3.json` marked "restored", H2/H3R acceptance gates recorded, drift sources documented.
- Acceptance gates from contract: ✅ H2 reproduced, ✅ H3R reproduced.

**R2 restore — H1 panel (2026-06-09, main):**
- `build-r2-h1-panel.mjs` (`npm run build-r2-h1-panel`): restores `r2_h1_panel.json`. Fixed 30-noun panel selected from WIL nominal entries (2–8 senses) present in ≥7/11 H1 dicts. Aggregates ALL L-blocks per stem per dict (removes MW headword-splitting artifact: MW per-lemma rises from 1.15 to 14.9). Pearson r=0.093 vs archived 0.01 (both near-flat; H1 NOT supported confirmed). Explicit-marker r=-0.035 vs archived 0.56 (archived was non-significant n=5; conclusion identical).
- 4 new tests (`stemKey`) → 134 total, all pass.
- `R2_REBUILD_CONTRACT.md`: `r2_h1_panel.json` marked "restored"; all required+recommended outputs now restored.
- Acceptance gates: ✅ H1R deconfounded (panel confirms full-corpus result, MW artifact removed).

**R2 restore — Explorer + H1 slice (2026-06-09, branch `feature/r2-explorer-h1-restore`):**
- `build-r2-source-anchors.mjs`: additive exports (behavior-preserving; 121 tests pass).
- `build-r2-explorer.mjs` (`npm run build-r2-explorer`): restores `senses_<dict>.jsonl` (14 dicts), `r2_align_<lemma>.json` (archived `{senses, alignments}` shape, 30-per-lemma cap), `r2_summary.json`. Key dharma `ap#4~ap90#4` J=1 alignment reproduced; cross-tradition PWG↔SKD/VCP aligns. Drift: 1811 vs 445 senseRows (PWK added + finer splitting; documented), 128 vs 104 alignmentRows (24-row PWK residual; documented).
- `build-r2-h1.mjs` (`npm run build-r2-h1`): restores `r2_h1.json`. Pearson 0.036 vs 0.06 (both ≈ 0; "no temporal inflation" reproduces). Entry counts exact all 11 dicts. Family ordering preserved (Benfey > Apte > MW ≈ Wilson > Cappeller > Petersburg > indigenous). Per-dict drift ≤13%, documented in code.
- 11 unit tests for `h1SenseUnits` + `buildLemmaPayload` → 121 total, all pass.
- Trust-block notices added to `/tools/r2-explorer` and `/tools/r2-h1`.
- `R2_REBUILD_CONTRACT.md` updated: Explorer+H1 outputs marked "restored", acceptance gates recorded, drift sources documented. Contract "Preservation" gate met (pages marked archived with live-data pointers).
- Acceptance gates from `R2_REBUILD_CONTRACT.md`: ✅ H1R reproduced, ✅ Explorer reproduced, ✅ Preservation. `r2_h2h3.json` and `r2_h1_panel.json` deferred to next slice.

- **Research & Practitioner Layer packet line** (merged to `main`): H4 semantic-field package + 105-row review packet; H5 anomaly taxonomy + 130-row reviewed sample + 10-row maker-QA source check + `divaraTa→diviraTa` proposal; H6 structural-register scatter + edge/family labels; xref hub review + source-check packet; three-axis comparison packet; R2 parser diagnostics + 5 decision packets + drift-explanation + checkpoint review. All generators emit empty human fields; all gated items tracked in `docs/USE_CASE_PAGE_ROADMAP.md`.
- **Microstructure m1–m8** (PRs #31–#34): subentry/preverb/xref/indigenous-root extractors, unified m5 profile, m6 xref lineage, Apte cf parsing, SKD anubandha key (verified from Durgādāsa front matter), KRM/YAT root recovery, m7 cross-dict agreement, m8 semantic fields. Validator `scripts/lexico/validate_lexico.py`.
- Earlier: Phases 1–2 (MW depth, dictionary comparison), review layer (gender/source-layer/siglum/alignment queues on a shared lib), DCS Phase 3a, navigation, rebuild-all consistency, first unit-test suite + CI. See `CHANGELOG.md`.

- 2026-06-17: restored .ai_state.md to tracking (org session-journal protocol; PR #148 had untracked it).
