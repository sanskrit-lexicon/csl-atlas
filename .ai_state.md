# Project Objective: Sanskrit Evidence Atlas

<!-- NB: local ../DCS/ export is reference data (text list + bibliography + 78k word→grammar list), NOT a passage corpus. See docs/DCS_SCHEMA.md. -->

Build an interactive, documentation-first research atlas connecting corpus evidence, grammar, and Sanskrit lexicography, where every claim is traceable `Claim -> Evidence -> Source`. Static-first (Observable Framework). See `ARCHITECTURE.md`, `CHANGELOG.md`.

Status (2026-06-09): Phases 1–2 complete; Phase 3a done (DCS = reference data, not passages). Microstructure line m1–m8 merged (PRs #31–#34). Research & Practitioner Layer (`docs/RESEARCH_LAYER_ROADMAP.md`, `docs/HYPOTHESIS_INDEX.md`) shipped a series of generated review packets — H4/H5/H6/xref/three-axis/R2-diagnostics — each: generator + JSON with **empty human fields** + doc + tests. That packet line is now exhausted; its four open items all wait on human/scholar/maker review (see Next Steps). 111 unit tests + CI.

## ➡️ Next Steps (Queue)

**Active engineering (this session, in progress): R2 restore — Explorer + H1 slice.** See WIP below. Sonnet fill-in queued.

**Human-gated (no code; tracked in `docs/USE_CASE_PAGE_ROADMAP.md` GTD board):**
- R2 checkpoint review — 10 `r2-checkpoint` rows need decisions in `src/data/review/r2-checkpoint-review.json` before any parser **promotion** (the 5 packet decisions). Guides: `docs/R2_CHECKPOINT_DECISIONS.md`, `docs/R2_CHECKPOINT_REVIEW.md`.
- H4 source review — 105 rows in `data/lexico/h4_semantic_field_review_packet.json`.
- H5 maker correction — submit `divaraTa → diviraTa` (`data/lexico/h5_maker_correction_proposal.json`) to makers; keep the other 9 source-supported rows out of correction flow.
- Xref source-check — adjudicate 40 shared-core + 10 prefix-control rows in `data/lexico/xref_source_check_packet.json`.

**Deferred R2 slices (after Explorer+H1):** `r2_h2h3.json` (H2 70%/54%, H3R copy/condense) — blocked because the archived 28-noun panel + edge config lived in the deleted `h2h3_analysis.py`; reconstruct from documented edges (WIL→SHS, WIL→YAT, AP90→AP) and document drift. Also `r2_h1_panel.json` (fixed-lemma deconfounding panel).

## 🚧 Current Work-In-Progress (WIP)

**R2 restore, Explorer + H1 slice (2026-06-09, scaffolded by Opus; fill-in for Sonnet).**

Context: the original `scripts/lexico/sense_split.py` / `h1_analysis.py` / `h2h3_analysis.py` and their outputs are GONE from the branch. Surviving inputs: archive fixtures `data/lexico/r2_archive_{explorer,h1}.json` (the target numbers) and the source-anchor prototype `scripts/build-r2-source-anchors.mjs` (loader + headword resolver + 4 parser-family split heuristics + Sanskrit-anchor alignment, but **anchor-lemmas only**). Contract: `docs/R2_REBUILD_CONTRACT.md`; gate: `docs/R2_REBUILD_EXPERIMENT.md`. **Guardrail honored:** reproduce archived numbers with the *already-documented* heuristics; do NOT change splitter rules (those are the deferred packet decisions). Boundary: stdlib/dictionary-source only, no DCS/LLM/backend.

Built + verified this session:
- Additive `export`s on `build-r2-source-anchors.mjs` (`splitRecord`, `anchorSets`, `cleanText`, `buildNormalRows`, `buildReverseRows`, `R2_DICTS`) — behavior-preserving; 111 tests still pass.
- `scripts/build-r2-explorer.mjs` (`npm run build-r2-explorer`) — **runs**. Emits `senses_<dict>.jsonl` (14 dicts), `r2_align_<lemma>.json` (5 lemmas, archived `{senses:{dict:[{sense,text,cluster,n_anchor}]}, alignments:[{a,b,j,shared,cross}]}` shape), `r2_summary.json`. Reproduces the headline `ap#4 ~ ap90#4` J=1.0 dharma alignment byte-for-byte. Drift vs archive: senseRows 1811 vs 445 (documented: PWK added + finer PWG/PWK/BEN splitting + MW homonym aggregation), alignmentRows 1673 vs 104.
- `scripts/build-r2-h1.mjs` (`npm run build-r2-h1`) — **runs**. Emits `r2_h1.json` (11 dict rows + 7 family means + Pearson r). **Entry counts match archived exactly for all 11 dicts.** Pearson −0.016 vs archived 0.06 → "no temporal inflation" conclusion reproduces. `cae` 1.355 = exact; most others within ~0.1.

Remaining fill-in (Sonnet — cheap, well-isolated):
1. **H1 `pwg` over-count:** 1.66 vs archived 1.161. The `div` explicit-marker counter counts every `<div>`, not just sense divisions. Calibrate `senseUnits()` in `build-r2-h1.mjs` (the one big delta; ben/ap90/ap are ~0.1–0.25 high, likely lumped-proxy `;`-clause tuning). Goal: land on archived values or document drift per row (the file already prints computed-vs-archived).
2. **Explorer alignment filter:** 1673 vs 104 — tighten so the 5-lemma alignment payload approximates the archived 104 (stricter strong-anchor / jaccard threshold), or document why source-backed exceeds it. `pw`/PWK is in the explorer senses but absent from the archived fixture — decide keep-and-document vs exclude.
3. **Validator + tests:** add `scripts/validate-...` (or extend an existing validator) and unit tests for `senseUnits`, `buildLemmaPayload`, `pearson`. Follow the existing `test/orchestrators.test.mjs` pattern (import the exported helpers — `main()` is already import-guarded).
4. **Pages:** regenerate `/tools/r2-explorer` + `/tools/r2-h1` from the restored JSON (currently static snapshots), or mark them clearly as archived-until-stable per the contract's Preservation gate. Add the compact trust block (`docs/CHART_TRUST_TEMPLATE.md`).
5. **Doc:** short trust-block doc for the restored slice; update `R2_REBUILD_CONTRACT.md` Required-Outputs table status and `R2_FINDINGS.md` "Next".
6. **Then** the deferred `r2_h2h3.json` + `r2_h1_panel.json` slices.

Not committed yet (user has not asked to commit). New untracked outputs: `r2_align_*.json`, `senses_*.jsonl`, `r2_h1.json`, `r2_summary.json`. NB: do NOT commit `.claude/` (harness/memory dir).

## 🧠 Dev Notes & Hypotheses

- R2 promotion gate vs reproduction: "parser promotion" = changing the 5 packet decision rules (div-source-scope, marker-run-scope, ae-reverse-bands, indigenous-iti-authority, source-gap-controls), deferred until the 10 checkpoint rows have human decisions. Reproducing the archived package with the *existing documented* heuristics is NOT promotion and is the allowed no-human work.
- Archive parity is a regression/comparison signal, NOT an optimization target — the contract says reproduce headline numbers OR explicitly document drift.
- Entry-count exact match in H1 confirms the corpus loader (`iterateDict`) is correct; remaining deltas are purely in the sense-unit *counting*, which is the documented calibration surface.
- Per-entry sense metric is confounded by headword-splitting policy (MW splits compounds into ~286k short entries → dilutes units/entry); the fixed-lemma panel (`r2_h1_panel.json`) removes it.
- `.ai_state.md` was stale for ~1 month (stopped at microstructure Round 12); the microstructure rounds are all in git history / PRs #31–#34 and `docs/MICROSTRUCTURE_*`. Compressed here on 2026-06-09.

## ✅ Completed (Recent only)

- **Research & Practitioner Layer packet line** (merged to `main`): H4 semantic-field package + 105-row review packet; H5 anomaly taxonomy + 130-row reviewed sample + 10-row maker-QA source check + `divaraTa→diviraTa` proposal; H6 structural-register scatter + edge/family labels; xref hub review + source-check packet; three-axis comparison packet; R2 parser diagnostics + 5 decision packets + drift-explanation + checkpoint review. All generators emit empty human fields; all gated items tracked in `docs/USE_CASE_PAGE_ROADMAP.md`.
- **Microstructure m1–m8** (PRs #31–#34): subentry/preverb/xref/indigenous-root extractors, unified m5 profile, m6 xref lineage, Apte cf parsing, SKD anubandha key (verified from Durgādāsa front matter), KRM/YAT root recovery, m7 cross-dict agreement, m8 semantic fields. Validator `scripts/lexico/validate_lexico.py`.
- Earlier: Phases 1–2 (MW depth, dictionary comparison), review layer (gender/source-layer/siglum/alignment queues on a shared lib), DCS Phase 3a, navigation, rebuild-all consistency, first unit-test suite + CI. See `CHANGELOG.md`.
