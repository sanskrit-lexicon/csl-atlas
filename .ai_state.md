# Project Objective: Sanskrit Evidence Atlas

<!-- NB: local ../DCS/ export is reference data (text list + bibliography + 78k word→grammar list), NOT a passage corpus. See docs/DCS_SCHEMA.md. -->


Build an interactive, documentation-first research atlas connecting corpus evidence, grammar, and Sanskrit lexicography, where every claim is traceable `Claim -> Evidence -> Source`. Static-first (Observable Framework). Phase order: docs (Phase 0) -> MW Quantitative Depth (Phase 1) -> Comparative Dictionary Lab (Phase 2) -> DCS corpus grammar (Phase 3). See `ARCHITECTURE.md`.

Status (2026-05-31): Phases 1–2 complete, Phase 3a done (DCS is reference data, not passages). Review layer has 4 queues on a shared lib. Site nav wired, all generated data consistent (rebuild-all), 39 unit tests + CI. See `CHANGELOG.md`. 28 PRs merged this run.

## ➡️ Next Steps (Queue)

Remaining work is mostly not code-doable by an agent:

- **Needs the maintainer (Russian):** bilingual EN/RU of the new tool pages. The atlas is otherwise EN/RU (`src/locales-{en,ru}.json`); the ~14 new pages are EN-only. Don't fabricate RU lexicographic terminology.
- **Needs a scholar:** work the four review queues (gender 4,556 · source-layer 449 · siglum-alias 151 · alignment 7) and the 50 TEI/OntoLex cases. Decisions are preserved across rebuilds by `reviewId`; resolving a source-layer or siglum item also improves the generated data.
- **Optional engineering:** dictionary↔DCS lemma coverage (UC-CG-14/15 — needs IAST→SLP1, capped at ~29% of DCS words by the `?` diacritic loss); full-corpus lemma lookup (the dossier covers only ≥5-dict lemmas to stay ~4 MB; broader needs a search index). Still no `LexemeHub` until real conflicts demand it.
- **Blocked:** passage-level corpus grammar (`CorpusOccurrence`, genre/period) awaits a passage-level DCS export — the local one is reference data only.
- **Housekeeping:** `csl-orig`/`DCS` drift slightly between sessions; rerun the generators (or a rebuild-all pass) before any release for a single consistent snapshot.

## 🚧 Current Work-In-Progress (WIP)

- **Microstructure line — issue #30 continuation (2026-06-04).** m1–m4 now MERGED (PR #31). Maintainer decisions logged in `docs/MICROSTRUCTURE_DECISIONS.md` (round 1): (1) next = **unified per-lemma profile** (join m1–m4 on `(dict,L)`); (2) **hold all papers** (data/tooling mode, no Article 9/10/12 yet); (3) **defer dashboard** (no EN/RU pages → no RU terminology needed now); (4) m4 = **deepen SKD/VCP quality** (decode SKD anubandha it-markers, split veṭ, trim gaṇa noise — NOT broaden to SHS/YAT yet). Two deliverables in flight: **(A)** unified profile, **(B)** m4 SKD/VCP deepening. Round 2 done: (5) SKD anubandhas decoded via **SKD∩VCP cross-walk** (VCP spells out what SKD encodes as Vopadeva/Kavikalpadruma it-letters — `aGa` SKD `i N`↔VCP `idit…Atma0`; `akza` SKD `U`=ūdit=veṭ), maintainer to adjudicate the proposed key (round 3); (6) profile scope = **all 43 dicts + per-dict fingerprint**; (7) **veṭ = own column** (`sew…vew`→set=1&vet=1; VCP `vew`=98 roots). Mechanical wins greenlit: veṭ column, VCP gaṇa short-forms `cu0/tu0/di0/ju0/sO0`. Out of scope by decision: papers, dashboard, more dicts. **Cross-walk DONE** (`scripts/lexico/_xwalk_skd_vcp.py`, 763 unambiguous 1:1 SKD∩VCP roots): independently reproduces the Pāṇinian anubandha→pada system — `Na`(ṅit)→ātmanepada 82%, `Ya`(ñit)→ubhayapada 96%, `i`→idit 90%, `u`(ūdit)→veṭ 40% [Pāṇ 1.3.12 / 1.3.72 / 7.1.58 / 7.2.44]; `ka`/`t`→ubhaya 92-94%; `Sa`/`Si`/`ya`/`O`/`na`/`ma`→para. Transitivity NOT anubandha-encoded (saka ~75% across all → lives in SKD Durgādāsa prose). Ambiguous: `f`(ṛ) 50/39 para/ātma, `I`(ī) 44/55. Round 3 = maintainer adjudicates this proposed key (pending). **BUILT this session (validated, NOT committed):** (B) m4 enhanced — `vet` column (veṭ, SKD 40/VCP 58), VCP gaṇa short-forms `cu0/tu0/ju0` (gaṇa 1,593→1,954), broadened prose transitivity (`sakarmmak`/`akarmmak` stems); SKD pada decode deliberately NOT applied (held). (A) **`m5_profile.py`** — unified per-lemma profile `microstructure_profile.csv` (60,328 rows over 22 dicts, lossless m1–m4 join on `(dict,L)`; m3 folded to `xref_out` out-degree) + `microstructure_fingerprint.json` (per-dict densities + dominant layer: **pwg/mw=xref, ap/pw/ben/ap90/stc=subentry, skd/vcp/krm=root** — the macro/micro thesis in one table). `validate_lexico.py` extended with m5 lossless-join check; all pass. Evidence: `scripts/lexico/_xwalk_skd_vcp.py`, key doc `docs/MICROSTRUCTURE_SKD_ANUBANDHA_KEY.md`. **Round 4 done:** ṛ-marker (#11) and gaṇa `di0`/`sO0` (#12) BOTH deferred — all parked on one gate: M.G.'s Kavikalpadruma/VCP-abbreviation verification. (#13) **Shipped as PR #33** — clean branch `feature/microstructure-profile` off `origin/main` via isolated worktree, lexico+docs only (NOT the local merged `feature/microstructure-subentries`, which carries an external `a4b2193` chore commit). `.ai_state.md` kept local (not in PR). **Single open gate now: M.G. verifies the proposed SKD anubandha→pada key, then I bake it into m4.** Other code-doable #30 seams still open & ungated: m3 cross-dict cross-ref graph overlap (lineage §3.1), AP/AP90/BEN `cf.` target parsing.
- **Round 5 (2026-06-04): m6 cross-ref lineage BUILT** (`scripts/lexico/m6_xref_lineage.py`, validated). §3.1: does MW `cf.` inherit PWG `Vgl.`? Normalizes m3 edges (unify PWG `°`/MW `-`, drop accents), intersects edge sets. **Finding: PARTIAL — 641 shared edges; MW 21.8%/PWG 9.1% on 2,538 shared-source lemmas (Jaccard 0.069); shared CORE (variant/cognate pointers) + independent expansion, NOT wholesale inheritance.** Doc `docs/MICROSTRUCTURE_XREF_LINEAGE.md`; outputs `xref_lineage.json`+`xref_shared_edges.csv`. NOT yet committed (PR #33 was the m5/m4 slice; m6 is new, uncommitted — ask whether to add to #33 or new PR). Remaining ungated seam: AP/AP90/BEN `cf.` parsing.
- **Round 7 (2026-06-04): Apte `cf.` parsing BUILT** (m3 extended; m5/m6 regenerated; validated). Decision #15: lemma edges + quotes to side file. AP **609**/AP90 **446**/CAE **196** new cf-lemma edges; **517 cf-quotes** → `xref_cf_quotes.csv`; **BEN=0** (cf. purely cognate). MW unchanged (7,665) → MW×PWG headline intact. **Positive control: AP×AP90=85.5%** (same dict, two editions) vs MW×PWG 21.8% — validates the lineage measure. m3 now 6 xref dicts; m5=61,356 rows; m6=10 pairs. **Ungated #30 seams now EXHAUSTED** — all remaining work (SKD pada, ṛ, di0/sO0) gated on M.G.'s Kavikalpadruma verification. To commit to PR #33.
- **Round 8 (2026-06-04): SKD KEY VERIFIED + re-decoded.** M.G. supplied `docs/refs/SKD_Front pages.docx` = Durgādāsa's **Dhātudīpikā 46-anubandha phala table** (read via pandoc). **Gate cleared.** Key insight: SKD anubandhas encode **gaṇa + operations, NOT pada** — the cross-walk's `ka`/`Sa`/`ya`/`na` "pada" signals are really gaṇa markers (curādi/tudādi/divādi/svādi); only ṅ/ñ mark pada. ṛ = caṅ a-hrasva (Pāṇ 7.4.2), confirmed operation-it. Re-probed (1,925/2,544 roots, faithful slot), then wired `_ANU_KEY`+`decode_anubandhas` into m4 (SKD only): **gaṇa 1,117→1,737, pada 1,167→1,498**, new `anubandhas` column. Rewrote `MICROSTRUCTURE_SKD_ANUBANDHA_KEY.md` as authoritative (cross-walk demoted to "empirical recovery, source-corrected"). Source also states "0/bindu = no anubandha" verbatim (primary-source footing for [[reference_skd_vcp_citation_style]]). m4/m5 regenerated, m1–m6 validate. **Open (round 9): SKD slot token `ma` (72×) not in the 46 anubandhas — what is it?** To commit round 8 to PR #33.
- **Round 9 (2026-06-04): `ma` RESOLVED + key complete.** M.G. supplied Palsule's KKD edition page: `ma` = `m`/mit = nici-hrasva (penultimate shortens before ṇic, Pāṇ 6.4.92), one of 17 anubandhas Vop. keeps with Pāṇinian sense. Added `ma`→op:nici-hrasva to `_ANU_KEY` (operation-it, no gaṇa/pada → CSV unchanged; only m4 key/docstring + m4_report caveats + key doc). Palsule corroborates the key (k/ṇ/p→curādi/phaṇādi/mucādi; au→aniṭ from Jainendra); 43 vs 46 count = tallying diff. **SKD anubandha line COMPLETE.** Committed round 8 (PR #33 @ 9f3a23e, 4 commits); round 9 to commit. PR #33 = microstructure m5/m6/Apte-cf/SKD-anubandha.
- **Round 10 (2026-06-04): KRM pada/trans recovered.** KRM does NOT use SKD's anubandha slot (slot=0 for all 1,757); it spells the same Kavikalpadruma grammar in a parenthesised cluster `(I-BvAdiH-792 saka-sew-parasmEpadI)`. Gaṇa was already caught (1,755 via `_GANA` full-form names), but the cluster's SHORT pada/trans forms (`para`/`A`/`uBa`, `saka`/`sa`/`aka`) were missed — only the ~170/50 full-form entries (`parasmEpadI`/`sakarmakaH`) resolved. Added `krm_cluster()` (strips tags, takes the parenthetical group(s) naming a gaṇa) + `_KRM_PADA`/`_KRM_TRANS` cluster-scoped detectors; the bare `A`=ātmanepada is safe inside the cluster. Wired a `code=="krm"` branch into `analyze_entry`. **KRM pada 170→1,378, trans 50→1,735** (gaṇa unchanged 1,755). Validated on aGi(bare-A→ātmane), aNka(uBa), awwa(uBayapadI/aka), aka(full forms, no regression). m4/m5 regenerated (m4 7,017 rows, m5 61,356), m1–m6 validate. CSV columns unchanged (values filled). Committed locally (`feature/microstructure-subentries` @ 0ccb3e2, ai-wip; rebased onto the collaborator's `18c2064` "structural register chart" — NOT force-pushed — and **pushed**) AND **ported to PR #33** (`feature/microstructure-profile` @ 4e65faf, pushed) via isolated worktree — profile's m1/m2/m3 inputs are byte-identical to this tree, so the same generated m4/m5 data validated unchanged there; `.ai_state.md` kept out of the PR as before. PR #33 now = m5/m6/Apte-cf/SKD-anubandha/KRM-cluster; summary + pushed-confirmation comments posted on the PR. The intentional dirty release-bucket tree was stash/popped across the rebase and is preserved.
- **Round 11 (2026-06-05, autonomous): YAT verbal roots recovered (issue #30 YAT seam).** YAT (Yates) read 0 under both root signals (cites no dhātupāṭha, marks no `sew`/`saka0`). Yates encodes the conjugation in its own block `{#(anubandhas) PRESENT-FORM#} <class>. {%pada.%}` (e.g. `{#(O-x) gacCati#} 1. {%a.%}` = gam, cl.1 bhvādi, parasmaipada). class digit = gaṇa; pada letter `a`=parasmaipada(-ti), `d`=ātmanepada(-te), `c`=ubhayapada. Added `_YAT_VB`/`yat_features()` + a `code=="yat"` branch; requiring a present form (ending ti/te) before the digit excludes Yates' gender-triple adjectives. Raw it-letters → `anubandhas`. **YAT 0→1,643 roots**; gaṇa bhvādi 1,009≫curādi 219>tudādi 139>divādi 117 & pada para 1,162>ātmane 369>ubhaya 112 (linguistically correct). m4 7,017→8,660, m5 61,356→62,999; validator allowlist extended; m1–m6 validate. Committed `feature/microstructure-subentries` @ 9adc8b0 (ai-wip). NOT yet ported to PR #33.
- **Round 12 (2026-06-05, autonomous): m7 cross-dict verbal-feature agreement (Article 9).** New `scripts/lexico/m7_root_agreement.py`: when ≥2 root dicts classify the same root, do they agree on gaṇa/pada/transitivity? Validates the five independent m4 parses against each other. Over 3,692 roots — **gaṇa 86.0% compatible** (208 conflict), pada 75.2% (347), trans 80.9% (239); "compatible" tolerates multi-class roots via per-(dict,root) label SETs. Framed as ANALYSIS not a review queue (homonym-split precedent; conflicts conflate disagreement + homonymy). Outputs `root_agreement.json` + `root_feature_conflicts.csv`; doc `docs/MICROSTRUCTURE_ROOT_AGREEMENT.md`; validator `validate_m7` added. m1–m7 validate. Committed @ ab70235 (ai-wip) + YAT-citation-convention finding (m7 reports `yat_citation_convention`: 953 YAT roots align only after restoring uccāraṇārtha -a; uniform strip collides homographs 86.0%→81.2%, so maintainer-gated, NOT applied). **Rounds 11+12 PORTED to PR #33** (`feature/microstructure-profile` @ 93b42c1, pushed) via worktree (profile inputs byte-identical, validated). **Issue #30 updated**: YAT checkbox checked, m7 added to Done table, comment posted (#issuecomment-4626157696). Local subentries pushed @ 181eae0.
- **Microstructure subentry inventory (Phase L0.6 → Article 9) — DONE this session**, on branch `feature/microstructure-subentries` (16 `ai-wip:` commits; **NOT** on main, the intentional dirty release-bucket tree is untouched — every commit verified to touch only lexico/data/docs). New `scripts/lexico/` line, outputs to `data/lexico/`. Results: `docs/MICROSTRUCTURE_M1_M2_RESULTS.md` + `docs/MICROSTRUCTURE_ZERO_MEANING.md` (what a "0" means — detector blindness, not absent content).
  - **m1_subentries.py** — uniform CASE-INSENSITIVE `<ab>`-marker inventory of 7 derivative-subentry categories (caus/pass/desid/intens/den/periphr/comp). Full `--all` run: **34,180 rows** over 1,495,459 entries / 43 dicts. Validated: MW `gam` = `[caus,pass,desid,intens]` (MICROSTRUCTURE §1.1). vderiv/verb **PW 3.77 · GRA 3.08 · BEN 2.55 · PWG 2.35 ≫ MW 0.48 · WIL 0.11**; confirms the macro/micro KPI (MW = many shallow separate-headword entries; Petersburg = few deep nests). **27/43 dicts at 0 by convention, not structurelessness** (MW72 0 across 55k = pre-`<ab>` 1872 edition; SKD/VCP indigenous; name/plant/epigraphy dicts). CLI `--probe/--sample/--all/--dicts`.
  - **m2_preverbs.py** — preverb (upasarga) subentries from the `<div n="p">— {#preverb#} {%gloss%}` convention (cats 7–10, which m1 misses). Clean totals **PWG 8,647 · PW 8,429 · CAE 3,303 · WIL 702** (the regex stops at `<`, excluding the shared-container `<ab>caus./partic.</ab>` form-blocks already in m1). MW=0 by design. Corpus preverb productivity: **vi · sam · pra · ā · pari · abhi · ud · upa · anu**. Validated PWG gam=62. Rows join m1 on `(dict,L)`. **CAE has preverb divs but its sibling CCS has 0** → microstructure-inheritance ≠ convention-inheritance (CAE/CCS near-identical in L0 headword conventions); feeds Paper H §5.
  - **m3_xrefs.py** — cross-reference edges (cat 24): PWG `<div n="v">` Vgl. {#t#} (**22,987**) + English `<ab>cf.</ab> <s>t</s>` (**MW 7,665**), both SLP1; cf.+`<lang>`/`<hom>` skipped; comma-lists split atomic. **30,654 edges**. Hub targets = compound *families* (a°/mahā°/su°/deva°). Only PWG uses `<div n=v>` (PW does not). Known gap: AP/AP90/BEN `cf.` uses non-`<s>` target markup, deferred. Feeds lineage roadmap §3.1.
  - **m4_indigenous.py (PROTOTYPE)** — the indigenous-tradition counterpart to m1 (the dicts m1 is blind to). **Two root signals** (`root_signal` col): (1) citation = dhātupāṭha named (`iti kavikalpadrumaH` 2,135× ⟹ root — carries SKD); (2) annotation = the seṭ/aniṭ + pada/transitivity cluster (`BvAdi0 Atma0 saka0 sew` — carries VCP, which names its source rarely). Pada/trans co-requirement blocks European false positives (verified ≈0, MW72=1). **SKD 2,544 · VCP 2,230 · KRM 1,757 (dedicated verb dict) · SHS 463** — all read 0 under m1's `<ab>`; all converge on ~dhātupāṭha size (~2k roots) = independent corroboration. **Emits columns** gaṇa/pada/transitivity/causative/seṭ-aniṭ (VCP's `0`-marked gaṇa forms `BvA0`/`curA0` disambiguate from `-Adi`; pada surface forms are `para0`/`pa0`/`Atma0`/`uBa0`). Distributions are **linguistically correct = validation**: VCP pada parasmaipada 1124>ubhaya 525>ātmane 248; gaṇa bhvādi 1152≫curādi 146>divādi 85>…; saka 1680>aka 503. SKD pada/trans coverage lower (anubandha it-markers undecoded). Validated on `aka`/`aGa`/`aca`. Empirically settles "0 = detector blindness, not absent verbs"; see `docs/MICROSTRUCTURE_ZERO_MEANING.md`.
  - **validate_lexico.py** — CSV↔aggregate + per-row invariant checks for m1–m4; all pass (34,180 / 3,128 / 30,654 / 7,017 rows).
  - **Corrected mid-build (probe-first paid off):** CDSL `<div>` is a flat self-closing TYPE marker (no `</div>`) — `<div n="vp"/>`=MW verbal-derivative block (wraps the `<ab>` markers, NOT idioms), `<div n="to"/>`=MW sense block, `<div n="v">`=PWG `Vgl.`/cross-ref (NOT verb subentries), `<div n="p">`=preverb. So `max_depth` is a marker-based 0/1/2 proxy. MW idioms (cat 11) stay untagged prose. **Compound subtypes largely UNtagged** (AP ~24 `Bah./Tat./Avyayī.` vs 5,051 generic `comp.`; only PWG `adj. comp.` 1,398 systematic) — negative result for Article 10; no m3 samāsa-typology seam.
  - **Caveats:** `n_subentries` = marker MENTIONS not distinct blocks (PWG repeats `caus.` per preverb block); `verbal_deriv_per_verbal` excludes comp/den, comparable only within similar `verbal` defs (MW `<info verb=` generous; CAE marks verbs w/o `<ab>cl.</ab>` so its m1 verbal=0 though m2 finds 3,190 preverb subs).
  - **Next:** (1) maintainer/M.G. to fold `scripts/lexico/` + `data/lexico/` + `docs/MICROSTRUCTURE_M1_M2_RESULTS.md` into the appropriate release bucket (or merge the branch) — left isolated to preserve the intentional dirty tree; (2) thread L0.6-done + div-type semantics into MICROSTRUCTURE-MACROSTRUCTURE.md §2 (its docs are untracked in the bucket); (3) further seams: parse AP/AP90/BEN `cf.` target markup (m3 gap); cross-dict cross-ref graph-overlap (PWG Vgl. set vs MW cf. set) for lineage §3.1; (4) m4 follow-ons — split veṭ (optional seṭ); decode SKD anubandha it-markers (its pada/transitivity coverage is lower than VCP's); tighten abbreviated-gaṇa noise (adA0/svA0); YAT (still 0 — uses neither signal). [DONE: VCP annotation root signal 43→2,230; gaṇa/pada/transitivity now emitted as columns, distributions linguistically correct.] Branch `feature/microstructure-subentries` = 16 commits, NOT pushed (interactive; awaiting go).
- Review-release roadmap active as of 2026-06-03: see `docs/REVIEW_RELEASE_ROADMAP.md`, `docs/RELEASE_CHECKLIST.md`, and `docs/LIGHT_REVIEW_SPRINT.md`.
- Current dirty tree is intentional and should be preserved. Treat it as release buckets: pilot exporter rename/generated outputs; PWA/discoverability changes; Reader Lookup v1 and locale/page polish; release-process docs.
- Reader Lookup v1 is static SLP1/IAST headword lookup, not full-text search, DCS coverage, Devanagari support, or `LexemeHub`.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- `main`'s README was a generated Cologne-tooling-runbook stub, diverged from the developed atlas README on the feature branch. Resolved the PR #2 merge conflict by keeping the atlas README and splicing in the stub's unique **Issues Overview** + **GitHub Issue Conventions** sections. Note: a future tooling-runbook run may regenerate/overwrite README — watch for that.
- Review-status vocabularies were inconsistent across the repo: `ARCHITECTURE.md` lists six (`machine`, `needs-review`, `reviewed-ok`, `reviewed-corrected`, `blocked`, `deferred`); `LOSS_REPORT_SCHEMA.md` lists three (`machine`, `reviewed`, `published`). Reconciled in `docs/REVIEW_REPORTS.md`: the six-status set is canonical; loss-report statuses are a subset; `published` is a downstream flag, not a review status. `data/schema/review-report.schema.json` encodes the canonical set.
- Real review files (`data/pilot/tei-review.json`, `review-cases.json`) use an envelope+`items` object shape, not the bare-array shape of `loss-report.schema.json`. The new review-report schema matches the envelope+items shape.
- `LexemeHub` and a generalized corpus model are deliberately NOT to be built before Phase 1/2 produce real constraints (AUC-08). Keep early phases small and deterministic; no runtime LLM classification.

## ✅ Completed (Recent only)

Documentation consolidation (branch `docs/consolidate`):

- Added `CHANGELOG.md` (covers the whole evidence-atlas build-out + interoperability baseline). Tidied this file per the session-end protocol (cleared WIP, rewrote Next Steps). Added currency notes to `HANDOFF.md` and `docs/PROJECT_SPEC.md` (interoperability-track docs that predate Phases 1–3) pointing to ARCHITECTURE/CHANGELOG. README links the changelog.

Remaining orchestrator guards + tests (branch `feature/orchestrator-tests-2`):

- All build orchestrators are now import-safe (guarded `main()`): added the guard to build-dictionary-coverage, build-dictionary-comparison, build-citation-apparatus, and the 3 review builders (the prior 3 were done earlier). Every `npm run build-*` still runs; importing is side-effect-free.
- Tested the biggest untested decision surface — `build-dictionary-coverage`'s `classify` (entry→type bucket priority) and `fitBand` (the all-dictionary fit-band scheme), plus `median`/`percent` and citation's `topForm`. Suite now **39 tests**, ~0.4s. Direct execution of build-coverage / build-alignment-review verified.

VCP/SKD prose gender (branch `feature/prose-gender`):

- `genderFromProse(body, code)` + `genderForDict(code, body)` in dict-parser: position-anchored extraction from the first marker token after `¦` (VCP `pu0`/`strI0`/`tri0`/`avya0`; SKD `puM,`/`klI,`/`tri,`). Hit rate: **SKD 92%** (all genders balanced), **VCP 56%** but skewed — m/adj/ind only, f/n barely marked at the anchor. VCP positives are reliable; the f/n gap only causes *missed* conflicts (safe), never false ones — so both are included.
- Wired `genderForDict` into build-dictionary-comparison (pos-disagreement + dossier now cover all 7) and build-gender-conflict-review (queue now all 7). Gender conflicts **3,671 → 4,556**; dossier now shows VCP/SKD gender. Pages + tests updated (35 tests).

Orchestrator tests (branch `feature/orchestrator-tests`):

- Made build orchestrators import-safe: guarded `main()` with `if (import.meta.url === pathToFileURL(process.argv[1]).href) main();` in build-mw-quantitative-depth, build-dcs-corpus, build-sense-depth — so `npm run build-*` still runs them, but tests can import without triggering the heavy source reads. (Pattern to copy for the other orchestrators when needed.)
- Exported + tested their decision helpers: `compareCounts` (MW count-divergence tolerance/warnings), `classifyGram` (DCS grammar grouping), `senseUnits` (max(1,markers) + regex-lastIndex safety). `test/orchestrators.test.mjs`, 8 tests; suite now 32, all pass in ~0.5s (confirms no source files are read).

Unit tests (branch `feature/unit-tests`):

- First test suite for the project: `test/lib.test.mjs`, 24 tests via Node's built-in `node:test` (zero new deps). Covers the pure deterministic logic everything rests on: mw parser/classifiers (incl. the grammar-exclusivity + lexicographer-only rules), source-layer base-form fallback, depth-graph, dict normalize/align (gender conflict, confidence), siglum fold + alias, and review-report preserve/envelope. `npm test` script + `.github/workflows/test.yml` (runs on PRs/push). All pass.

Rebuild-all consistency pass (branch `chore/rebuild-all`):

- Reran every source-derived generator from one current `csl-orig`/`DCS` snapshot, in dependency order: coverage → mw-depth → dict-comparison → citation-apparatus (+siglum review) → sense-depth → dcs → gender/source-layer/alignment reviews. (Interoperability pilot left as-is — separate validated artifact.) All validators + `npm run build` pass.
- Finding: source barely drifted — coverage total 1,495,461 → 1,495,459 (−2 records), alignment high-confidence +2. Almost everything else was timestamp-only, confirming the session's data was already current. Committed the full snapshot so all generated data shares one rebuild moment.

Navigation + landing integration (branch `feature/navigation`):

- The ~13 tool pages added this session were unreachable (in README only, not the sidebar/landing). Reorganized `observablehq.config.js` into purpose groups: MW depth (Phase 1), Dictionary comparison (Phase 2), Corpus (Phase 3), Review queues, Interoperability & figures, Paper, Dictionaries. Added an "Evidence atlas — quantitative tracks" card section to `src/index.md`.
- `npm run build` link validation rose 90 → 106. Still EN-only — bilingual EN/RU of the new pages remains the next reader-facing gap.

Phase 3a — DCS schema inspection + corpus inventory (branch `feature/dcs-phase3a`):

- Honoured the plan's documentation-first gate. Found the local `../DCS/` export and documented it in `docs/DCS_SCHEMA.md`. **Key finding: it's reference data, not passages** — `DCS-abbreviation-list.txt` (189 texts), `DCS-Corpus-Bibliography` (free text, ~few detailed), `DCS-72034-gramm-tag-stats.csv` (78,761 word→grammar rows; semicolon-sep `#;Word;GRAM`; 40 categories incl verb class+pada; **71% of words carry `?` diacritic loss**).
- `scripts/build-dcs-corpus.mjs` (`npm run build-dcs-corpus`) → `src/data/corpus/dcs-manifest.json`: 184 texts (2 bibliography-enriched) + aggregate grammar-group profile (nominal 48k, adj 15k, verbal 6.9k, ind 1.5k). Page `src/tools/dcs-corpus-inventory.md`.
- Did NOT build CorpusOccurrence/passage dashboards (not in this export) — revised the plan doc to say so. Updated `docs/DCS_CORPUS_INGESTION_PLAN.md` with the Done note.

Sense-depth comparison (branch `feature/sense-depth`, UC-CD-06):

- Sense segmentation is structural only in AP (`∙`), PWG/PWK (`<div>`); added `senseSegmented` + `SENSE_MARKER` to manifest. **MW excluded** (verified `<div>` only ~0.05/entry — its senses are prose, so a structural count would falsely make MW sense-poor); WIL/VCP/SKD prose too.
- `scripts/build-sense-depth.mjs` (`npm run build-sense-depth`) → `src/data/dicts/sense-depth.json`: per-dict richness (AP densest 1.74/entry, PWG 1.66, PWK 1.62), deepest-treatment leaderboard (PWK wins 12,689), and 10,375 cross-dict gaps. Page `src/tools/dictionary-senses.md`.
- Caveat documented: AP's bullet unit ≠ PWG/PWK's `<div>`, so AP-vs-Petersburg gaps partly reflect encoding; PWG-vs-PWK is the cleanest pair. senseUnits = max(1, marker count); proxy, not curated sense inventory.

Cross-dictionary siglum mapping (branch `feature/siglum-mapping`):

- `scripts/lib/source-siglum.mjs`: `foldSiglum` (NFD + strip diacritics/case/non-alnum) auto-aligns MBh/MBH→mbh, RV/ṚV→rv; `canonicalSiglum` adds a reviewed alias layer (`src/data/dict-source-aliases.json`) for scheme differences (bhag→bhp). Fold alone lifted MW/PWG shared sigla 630→955.
- `build-citation-apparatus.mjs` now emits the previously-deferred **cross-dict source × dictionary matrix** + pairwise overlap (canonicalized): MBh, ṚV, Bhāgavata-purāṇa (via alias) now align across all 4 tagged dicts. Citations page shows the matrix + a Jaccard heatmap.
- **Alias-growing review queue** (`source-siglum-alias`, added to schema enum): single-dictionary high-frequency sigla (≥100) → `src/data/review/source-siglum-review.json` via the review lib; page `src/tools/review-source-siglum.md`. 151 candidates, correctly surfacing real alias work (Pāṇ=P, Mn=Ms, Rājat=RĀJA-TAR). Closes the loop: add an alias → siglum aligns and leaves the queue.

Citation-apparatus comparison (branch `feature/citation-apparatus`, UC-CD-07):

- `<ls>` is used by MW/AP/PWG/PWK; WIL is untagged (227); VCP/SKD cite in prose via `iti`. Added `citationTagged` to the manifest.
- `scripts/build-citation-apparatus.mjs` (`npm run build-citation-apparatus`) → `src/data/dicts/citation-apparatus.json`: per-dict density, apparatus breadth (distinct base sigla), and each dict's top sources. Page `src/tools/dictionary-citations.md`. Headline: **PWG ~6.26 citations/entry** vs MW 1.12, AP 0.75, PWK 0.58.
- **Key decision — did NOT ship a cross-dict source matrix/overlap**: dictionaries use different siglum conventions (MW `MBh` vs PWG `MBH`, `RV` vs `ṚV`), so naive matching is wrong (the `MBh` row showed PWG=0). Reused `baseForm` to strip loci (`MBh. iii,5`→`MBh`) but case/diacritic alignment across dicts needs a reviewed siglum-mapping table — deferred and documented, rather than ship misleading overlap numbers.

Homonym-split detection (branch `feature/homonym-split`):

- `<h>` is used only by MW, PWG, PWK (5.7k/6.5k/8k markers); AP/WIL/VCP/SKD don't mark homonyms. Added `homonymMarked` to the manifest; extended `dict-parser.iterateDict` to yield `h`.
- `build-dictionary-comparison.mjs` now emits `src/data/dicts/homonym-split.json`: lemmas where the homonym-marking dicts disagree on homonym count (distinct `<h>`, or 1 if unmarked), candidate when present in ≥2 of them with differing counts and max ≥ 2. **3,586 candidates** (e.g. `arj` MW=1/PWG=5/PWK=2). Page `src/tools/dictionary-homonyms.md`.
- Framed as analysis (UC-LX-10), NOT a review queue — differing homonymy is usually legitimate lexicography, not an error. Validator extended.

Review layer — shared lib + alignment queue (branch `feature/alignment-review`):

- Extracted `scripts/lib/review-report.mjs` (`loadPreserved` / `reviewFields` / `reviewPayload` / `writeReport`) and refactored the gender + source-layer generators onto it — verified byte-identical output (diff modulo `generatedAt`).
- New `scripts/build-alignment-review.mjs` (`npm run build-alignment-review`) reads `src/data/dicts/alignment-confidence.json` and emits the `low-confidence-alignment` queue. Finding: only **7** medium cases exist (vs 174,787 high) — SLP1 headwords align byte-identically, so this queue is intrinsically tiny; the 7 are headwords differing only by a nasalization mark (`o` vs `o~`). Framed as the UC-CD-09 alignment-confidence dashboard on `src/tools/review-alignment.md`.
- Enhanced `build-dictionary-comparison.mjs`: the alignment-confidence output now carries the **complete** `lowConfidence` list (renamed from the capped `reviewQueueSample`) with per-dict source links.

Review layer — unknown-source-layer queue (branch `feature/source-layer-review`):

- `scripts/build-source-layer-review.mjs` (`npm run build-source-layer-review`) lists MW `<ls>` sigla that resolve to `unknown` (after editorial + base-form fallback) cited >= 5 times → 449 review items in `src/data/review/unknown-source-layers-review.json` (8,293 distinct unknown total; rarer omitted as artifacts). Page `src/tools/review-source-layers.md`.
- Closes a loop: resolving an item by adding the source to `src/data/mw-source-layers.json` removes it from the queue and improves the MW diachronic profile on the next `build-mw-depth`. Same schema + reviewId-preservation as the gender queue; `validate-review-reports` covers both files.

Review layer — gender-conflict queue (branch `feature/gender-conflict-review`):

- `scripts/build-gender-conflict-review.mjs` (`npm run build-gender-review`) re-derives all 3,671 cross-dictionary gender conflicts from the 5 tagged dicts and emits `src/data/review/gender-conflicts-review.json` conforming to `data/schema/review-report.schema.json`.
- **Overlay preservation verified**: seeded a `reviewed-corrected`/reviewer/note on one item, rebuilt, confirmed it survived (preserved by `reviewId`); only machine fields refresh. This is the key review-layer invariant.
- `scripts/validate-review-reports.mjs` (`npm run validate-review-reports`) is a data-driven schema check (reads enums/required from the schema; no ajv dep). Page `src/tools/review-gender-conflicts.md` (filter by status/lemma). REVIEW_REPORTS.md gained an "Implemented queues" table.
- Single output copy under `src/data/review/` only (~4.6 MB pretty, human-editable); dropped the `data/review/` duplicate to avoid double-committing a large file.

Per-lemma dossier (branch `feature/dictionary-dossier`):

- Extended `scripts/build-dictionary-comparison.mjs` to emit `src/data/dicts/lemma-dossier.json` from the existing index; page `src/tools/dictionary-dossier.md` (client-side search). Validator updated.
- Size lesson: full hrefs + pretty-print over 52,933 entries (≥4 dicts) = 58 MB. Fixed by raising threshold to ≥5 dicts (28,512 lemmas), compact tuple form `[code, records, firstLine, gender]`, line-numbers-not-hrefs (page rebuilds URLs from `hrefBase`), and no indentation → ~4 MB. Lower-coverage lemmas deliberately excluded (would need a search backend).

Phase 2 — Comparative Dictionary Lab, first slice (branch `feature/dictionary-comparison`):

- Libs `scripts/lib/dict-{normalize,manifest,parser,align}.mjs`; orchestrator `scripts/build-dictionary-comparison.mjs`; validator `scripts/validate-dictionary-comparison.mjs`; npm `build-dict-comparison` / `validate-dict-comparison`.
- Indexes 7 dicts (mw, ap, pwg, **pw**=PWK, wil, vcp, skd; ~808k records) by normalized SLP1 `<k1>` → 298,427 distinct lemmas. Outputs in `src/data/dicts/` (coverage-matrix, pairwise-overlap, all-intersection, dictionary-unique, pos-disagreement, alignment-confidence, validation). Pages `dictionary-{coverage-matrix,overlap,conflicts}.md`.
- Findings: 1,913 lemmas in all 7; PWG/PWK highest overlap (Jaccard 0.63); 3,671 tagged-dict gender conflicts (e.g. aMSin AP=m vs PWG=f); alignment confidence almost all `high` (SLP1 headwords align byte-identically — a real result, not a gap). `build`/`validate-dict-comparison`/`npm run build` (90 links) pass.
- Key gotcha recorded: PWK source code is `pw`, not `pwk`.

Phase 1 — MW Quantitative Depth (branch `feature/mw-quantitative-depth`):

- Libs `scripts/lib/mw-{parser,classifiers,source-layers,depth-graph}.mjs`; orchestrator `scripts/build-mw-quantitative-depth.mjs`; validator `scripts/validate-mw-depth.mjs`; npm scripts `build-mw-depth` / `validate-mw-depth`.
- Classifier reproduces committed type counts EXACTLY after two findings: (1) grammar types attach only to primary entries (not compound/derived/continuation), (2) grammar types are mutually exclusive by priority m>f>n>mfn>ind. Remaining ±1–7 diffs are source-version drift (committed counts are from mw.txt 2026-05-23; local file is 2026-05-29, 286560 vs 286561 records).
- Outputs in `src/data/mw/` (7 JSONs) + seed `src/data/mw-source-layers.json`. Pages `src/tools/mw-{depth-dashboard,diachronic-layers,family-depth}.md`. `npm run build-mw-depth`, `validate-mw-depth`, and `npm run build` (90 links) all pass.

Session 2026-05-29 — documentation follow-up + interoperability merge:

- Authored the 5 shortlist docs from `docs/USE_CASES.md`: `DICTIONARY_USER_GUIDE.md`, `EVIDENCE_LABELS.md`, `REVIEW_REPORTS.md`, `DICTIONARY_COMPARISON_PLAN.md`, `DCS_CORPUS_INGESTION_PLAN.md`. Wired them into `ARCHITECTURE.md` and the README.
- **PR #3** (merge `4d62636`): docs foundation + 5 new docs, split off `main` for standalone review.
- **PR #2** (merge `11cf7c2`): merged the full `interoperability-handoff` branch (interoperability track, 50-case TEI/OntoLex pilot data, coverage layer, bilingual UI, atlas README) into `main`; refreshed against `main` first (README conflict resolved as above).
- **PR #9** (merge `a575c31`): added `data/schema/review-report.schema.json` stub (canonical status vocab + evidence levels + queue/subject vocab from `REVIEW_REPORTS.md`).
- **PR #10** (merge `f017b04`): updated `docs/REVIEW_REPORTS.md` to reference the schema file instead of "should be added."
- Discarded a pre-existing, unrelated uncommitted edit to `docs/GEMINI_EVIDENCE_ATLAS_HANDOFF.md` (not authored this session).
- Outstanding from README checklist: human philological review of all 50 TEI/OntoLex cases (not started).
